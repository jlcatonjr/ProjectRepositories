<!-- AGENTTEAMS:BEGIN content v=1 -->
# SETUP-REQUIRED.md

The following **1490 placeholder(s)** could not be automatically resolved
for project **ProjectRepositories** and require manual attention.

---

## 1. `{CONVERSION_PIPELINE}`

**Found in:** `multiple`
**Context:** The placeholder {CONVERSION_PIPELINE} could not be auto-resolved.

**Action required:** Search for `{MANUAL:CONVERSION_PIPELINE}` across all generated
agent files and replace with the correct value.

---

## 2. `{PIP_PACKAGE_NAME}`

**Found in:** `multiple`
**Context:** The placeholder {PIP_PACKAGE_NAME} could not be auto-resolved.

**Action required:** Search for `{MANUAL:PIP_PACKAGE_NAME}` across all generated
agent files and replace with the correct value.

---

## 3. `{DOC_SITE_CONFIG_FILE}`

**Found in:** `multiple`
**Context:** The placeholder {DOC_SITE_CONFIG_FILE} could not be auto-resolved.

**Action required:** Search for `{MANUAL:DOC_SITE_CONFIG_FILE}` across all generated
agent files and replace with the correct value.

---

## 4. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-anacondadepends-reference.md`
**Context:** The reference file for '_anaconda_depends' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 5. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-anacondadepends-reference.md`
**Context:** The reference file for '_anaconda_depends' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 6. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-anacondadepends-reference.md`
**Context:** The reference file for '_anaconda_depends' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 7. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-ipywjlabnbextconf-reference.md`
**Context:** The reference file for '_ipyw_jlab_nb_ext_conf' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 8. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-ipywjlabnbextconf-reference.md`
**Context:** The reference file for '_ipyw_jlab_nb_ext_conf' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 9. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-ipywjlabnbextconf-reference.md`
**Context:** The reference file for '_ipyw_jlab_nb_ext_conf' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 10. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-alabaster-reference.md`
**Context:** The reference file for 'alabaster' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 11. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-alabaster-reference.md`
**Context:** The reference file for 'alabaster' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 12. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-alabaster-reference.md`
**Context:** The reference file for 'alabaster' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 13. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-anaconda-reference.md`
**Context:** The reference file for 'anaconda' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 14. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-anaconda-reference.md`
**Context:** The reference file for 'anaconda' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 15. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-anaconda-reference.md`
**Context:** The reference file for 'anaconda' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 16. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-anaconda-anon-usage-reference.md`
**Context:** The reference file for 'anaconda-anon-usage' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 17. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-anaconda-anon-usage-reference.md`
**Context:** The reference file for 'anaconda-anon-usage' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 18. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-anaconda-anon-usage-reference.md`
**Context:** The reference file for 'anaconda-anon-usage' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 19. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-anaconda-client-reference.md`
**Context:** The reference file for 'anaconda-client' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 20. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-anaconda-client-reference.md`
**Context:** The reference file for 'anaconda-client' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 21. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-anaconda-client-reference.md`
**Context:** The reference file for 'anaconda-client' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 22. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-anaconda-cloud-auth-reference.md`
**Context:** The reference file for 'anaconda-cloud-auth' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 23. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-anaconda-cloud-auth-reference.md`
**Context:** The reference file for 'anaconda-cloud-auth' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 24. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-anaconda-cloud-auth-reference.md`
**Context:** The reference file for 'anaconda-cloud-auth' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 25. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-anaconda-navigator-reference.md`
**Context:** The reference file for 'anaconda-navigator' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 26. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-anaconda-navigator-reference.md`
**Context:** The reference file for 'anaconda-navigator' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 27. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-anaconda-navigator-reference.md`
**Context:** The reference file for 'anaconda-navigator' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 28. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-anaconda-project-reference.md`
**Context:** The reference file for 'anaconda-project' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 29. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-anaconda-project-reference.md`
**Context:** The reference file for 'anaconda-project' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 30. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-anaconda-project-reference.md`
**Context:** The reference file for 'anaconda-project' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 31. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-ansi2html-reference.md`
**Context:** The reference file for 'ansi2html' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 32. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-ansi2html-reference.md`
**Context:** The reference file for 'ansi2html' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 33. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-ansi2html-reference.md`
**Context:** The reference file for 'ansi2html' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 34. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-anyio-reference.md`
**Context:** The reference file for 'anyio' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 35. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-anyio-reference.md`
**Context:** The reference file for 'anyio' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 36. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-anyio-reference.md`
**Context:** The reference file for 'anyio' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 37. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-appdirs-reference.md`
**Context:** The reference file for 'appdirs' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 38. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-appdirs-reference.md`
**Context:** The reference file for 'appdirs' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 39. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-appdirs-reference.md`
**Context:** The reference file for 'appdirs' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 40. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-arch-reference.md`
**Context:** The reference file for 'arch' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 41. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-arch-reference.md`
**Context:** The reference file for 'arch' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 42. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-arch-reference.md`
**Context:** The reference file for 'arch' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 43. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-argon2-cffi-reference.md`
**Context:** The reference file for 'argon2-cffi' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 44. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-argon2-cffi-reference.md`
**Context:** The reference file for 'argon2-cffi' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 45. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-argon2-cffi-reference.md`
**Context:** The reference file for 'argon2-cffi' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 46. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-argon2-cffi-bindings-reference.md`
**Context:** The reference file for 'argon2-cffi-bindings' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 47. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-argon2-cffi-bindings-reference.md`
**Context:** The reference file for 'argon2-cffi-bindings' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 48. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-argon2-cffi-bindings-reference.md`
**Context:** The reference file for 'argon2-cffi-bindings' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 49. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-arrow-reference.md`
**Context:** The reference file for 'arrow' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 50. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-arrow-reference.md`
**Context:** The reference file for 'arrow' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 51. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-arrow-reference.md`
**Context:** The reference file for 'arrow' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 52. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-arrow-cpp-reference.md`
**Context:** The reference file for 'arrow-cpp' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 53. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-arrow-cpp-reference.md`
**Context:** The reference file for 'arrow-cpp' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 54. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-arrow-cpp-reference.md`
**Context:** The reference file for 'arrow-cpp' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 55. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-astroid-reference.md`
**Context:** The reference file for 'astroid' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 56. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-astroid-reference.md`
**Context:** The reference file for 'astroid' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 57. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-astroid-reference.md`
**Context:** The reference file for 'astroid' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 58. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-astropy-reference.md`
**Context:** The reference file for 'astropy' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 59. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-astropy-reference.md`
**Context:** The reference file for 'astropy' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 60. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-astropy-reference.md`
**Context:** The reference file for 'astropy' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 61. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-atomicwrites-reference.md`
**Context:** The reference file for 'atomicwrites' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 62. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-atomicwrites-reference.md`
**Context:** The reference file for 'atomicwrites' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 63. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-atomicwrites-reference.md`
**Context:** The reference file for 'atomicwrites' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 64. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-attrs-reference.md`
**Context:** The reference file for 'attrs' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 65. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-attrs-reference.md`
**Context:** The reference file for 'attrs' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 66. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-attrs-reference.md`
**Context:** The reference file for 'attrs' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 67. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-automat-reference.md`
**Context:** The reference file for 'automat' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 68. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-automat-reference.md`
**Context:** The reference file for 'automat' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 69. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-automat-reference.md`
**Context:** The reference file for 'automat' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 70. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-autopep8-reference.md`
**Context:** The reference file for 'autopep8' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 71. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-autopep8-reference.md`
**Context:** The reference file for 'autopep8' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 72. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-autopep8-reference.md`
**Context:** The reference file for 'autopep8' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 73. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-aws-c-common-reference.md`
**Context:** The reference file for 'aws-c-common' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 74. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-aws-c-common-reference.md`
**Context:** The reference file for 'aws-c-common' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 75. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-aws-c-common-reference.md`
**Context:** The reference file for 'aws-c-common' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 76. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-aws-c-event-stream-reference.md`
**Context:** The reference file for 'aws-c-event-stream' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 77. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-aws-c-event-stream-reference.md`
**Context:** The reference file for 'aws-c-event-stream' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 78. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-aws-c-event-stream-reference.md`
**Context:** The reference file for 'aws-c-event-stream' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 79. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-aws-checksums-reference.md`
**Context:** The reference file for 'aws-checksums' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 80. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-aws-checksums-reference.md`
**Context:** The reference file for 'aws-checksums' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 81. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-aws-checksums-reference.md`
**Context:** The reference file for 'aws-checksums' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 82. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-aws-sdk-cpp-reference.md`
**Context:** The reference file for 'aws-sdk-cpp' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 83. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-aws-sdk-cpp-reference.md`
**Context:** The reference file for 'aws-sdk-cpp' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 84. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-aws-sdk-cpp-reference.md`
**Context:** The reference file for 'aws-sdk-cpp' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 85. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-babel-reference.md`
**Context:** The reference file for 'babel' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 86. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-babel-reference.md`
**Context:** The reference file for 'babel' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 87. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-babel-reference.md`
**Context:** The reference file for 'babel' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 88. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-backcall-reference.md`
**Context:** The reference file for 'backcall' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 89. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-backcall-reference.md`
**Context:** The reference file for 'backcall' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 90. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-backcall-reference.md`
**Context:** The reference file for 'backcall' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 91. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-backports-reference.md`
**Context:** The reference file for 'backports' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 92. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-backports-reference.md`
**Context:** The reference file for 'backports' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 93. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-backports-reference.md`
**Context:** The reference file for 'backports' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 94. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-backportsfunctoolslrucache-reference.md`
**Context:** The reference file for 'backports.functools_lru_cache' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 95. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-backportsfunctoolslrucache-reference.md`
**Context:** The reference file for 'backports.functools_lru_cache' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 96. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-backportsfunctoolslrucache-reference.md`
**Context:** The reference file for 'backports.functools_lru_cache' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 97. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-backportstempfile-reference.md`
**Context:** The reference file for 'backports.tempfile' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 98. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-backportstempfile-reference.md`
**Context:** The reference file for 'backports.tempfile' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 99. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-backportstempfile-reference.md`
**Context:** The reference file for 'backports.tempfile' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 100. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-backportsweakref-reference.md`
**Context:** The reference file for 'backports.weakref' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 101. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-backportsweakref-reference.md`
**Context:** The reference file for 'backports.weakref' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 102. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-backportsweakref-reference.md`
**Context:** The reference file for 'backports.weakref' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 103. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-bcrypt-reference.md`
**Context:** The reference file for 'bcrypt' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 104. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-bcrypt-reference.md`
**Context:** The reference file for 'bcrypt' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 105. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-bcrypt-reference.md`
**Context:** The reference file for 'bcrypt' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 106. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-beautifulsoup4-reference.md`
**Context:** The reference file for 'beautifulsoup4' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 107. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-beautifulsoup4-reference.md`
**Context:** The reference file for 'beautifulsoup4' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 108. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-beautifulsoup4-reference.md`
**Context:** The reference file for 'beautifulsoup4' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 109. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-binaryornot-reference.md`
**Context:** The reference file for 'binaryornot' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 110. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-binaryornot-reference.md`
**Context:** The reference file for 'binaryornot' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 111. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-binaryornot-reference.md`
**Context:** The reference file for 'binaryornot' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 112. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-bitarray-reference.md`
**Context:** The reference file for 'bitarray' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 113. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-bitarray-reference.md`
**Context:** The reference file for 'bitarray' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 114. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-bitarray-reference.md`
**Context:** The reference file for 'bitarray' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 115. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-bkcharts-reference.md`
**Context:** The reference file for 'bkcharts' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 116. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-bkcharts-reference.md`
**Context:** The reference file for 'bkcharts' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 117. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-bkcharts-reference.md`
**Context:** The reference file for 'bkcharts' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 118. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-black-reference.md`
**Context:** The reference file for 'black' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 119. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-black-reference.md`
**Context:** The reference file for 'black' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 120. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-black-reference.md`
**Context:** The reference file for 'black' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 121. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-blas-reference.md`
**Context:** The reference file for 'blas' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 122. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-blas-reference.md`
**Context:** The reference file for 'blas' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 123. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-blas-reference.md`
**Context:** The reference file for 'blas' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 124. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-bleach-reference.md`
**Context:** The reference file for 'bleach' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 125. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-bleach-reference.md`
**Context:** The reference file for 'bleach' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 126. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-bleach-reference.md`
**Context:** The reference file for 'bleach' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 127. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-blosc-reference.md`
**Context:** The reference file for 'blosc' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 128. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-blosc-reference.md`
**Context:** The reference file for 'blosc' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 129. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-blosc-reference.md`
**Context:** The reference file for 'blosc' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 130. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-bokeh-reference.md`
**Context:** The reference file for 'bokeh' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 131. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-bokeh-reference.md`
**Context:** The reference file for 'bokeh' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 132. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-bokeh-reference.md`
**Context:** The reference file for 'bokeh' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 133. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-boltons-reference.md`
**Context:** The reference file for 'boltons' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 134. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-boltons-reference.md`
**Context:** The reference file for 'boltons' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 135. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-boltons-reference.md`
**Context:** The reference file for 'boltons' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 136. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-boost-cpp-reference.md`
**Context:** The reference file for 'boost-cpp' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 137. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-boost-cpp-reference.md`
**Context:** The reference file for 'boost-cpp' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 138. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-boost-cpp-reference.md`
**Context:** The reference file for 'boost-cpp' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 139. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-boto3-reference.md`
**Context:** The reference file for 'boto3' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 140. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-boto3-reference.md`
**Context:** The reference file for 'boto3' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 141. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-boto3-reference.md`
**Context:** The reference file for 'boto3' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 142. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-botocore-reference.md`
**Context:** The reference file for 'botocore' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 143. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-botocore-reference.md`
**Context:** The reference file for 'botocore' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 144. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-botocore-reference.md`
**Context:** The reference file for 'botocore' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 145. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-bottleneck-reference.md`
**Context:** The reference file for 'bottleneck' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 146. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-bottleneck-reference.md`
**Context:** The reference file for 'bottleneck' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 147. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-bottleneck-reference.md`
**Context:** The reference file for 'bottleneck' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 148. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-branca-reference.md`
**Context:** The reference file for 'branca' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 149. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-branca-reference.md`
**Context:** The reference file for 'branca' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 150. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-branca-reference.md`
**Context:** The reference file for 'branca' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 151. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-brotli-reference.md`
**Context:** The reference file for 'brotli' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 152. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-brotli-reference.md`
**Context:** The reference file for 'brotli' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 153. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-brotli-reference.md`
**Context:** The reference file for 'brotli' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 154. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-brotli-bin-reference.md`
**Context:** The reference file for 'brotli-bin' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 155. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-brotli-bin-reference.md`
**Context:** The reference file for 'brotli-bin' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 156. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-brotli-bin-reference.md`
**Context:** The reference file for 'brotli-bin' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 157. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-brotlipy-reference.md`
**Context:** The reference file for 'brotlipy' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 158. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-brotlipy-reference.md`
**Context:** The reference file for 'brotlipy' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 159. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-brotlipy-reference.md`
**Context:** The reference file for 'brotlipy' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 160. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-bt-reference.md`
**Context:** The reference file for 'bt' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 161. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-bt-reference.md`
**Context:** The reference file for 'bt' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 162. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-bt-reference.md`
**Context:** The reference file for 'bt' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 163. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-bzip2-reference.md`
**Context:** The reference file for 'bzip2' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 164. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-bzip2-reference.md`
**Context:** The reference file for 'bzip2' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 165. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-bzip2-reference.md`
**Context:** The reference file for 'bzip2' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 166. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-c-ares-reference.md`
**Context:** The reference file for 'c-ares' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 167. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-c-ares-reference.md`
**Context:** The reference file for 'c-ares' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 168. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-c-ares-reference.md`
**Context:** The reference file for 'c-ares' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 169. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-ca-certificates-reference.md`
**Context:** The reference file for 'ca-certificates' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 170. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-ca-certificates-reference.md`
**Context:** The reference file for 'ca-certificates' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 171. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-ca-certificates-reference.md`
**Context:** The reference file for 'ca-certificates' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 172. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-certifi-reference.md`
**Context:** The reference file for 'certifi' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 173. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-certifi-reference.md`
**Context:** The reference file for 'certifi' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 174. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-certifi-reference.md`
**Context:** The reference file for 'certifi' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 175. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-cffi-reference.md`
**Context:** The reference file for 'cffi' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 176. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-cffi-reference.md`
**Context:** The reference file for 'cffi' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 177. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-cffi-reference.md`
**Context:** The reference file for 'cffi' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 178. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-cfitsio-reference.md`
**Context:** The reference file for 'cfitsio' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 179. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-cfitsio-reference.md`
**Context:** The reference file for 'cfitsio' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 180. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-cfitsio-reference.md`
**Context:** The reference file for 'cfitsio' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 181. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-chardet-reference.md`
**Context:** The reference file for 'chardet' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 182. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-chardet-reference.md`
**Context:** The reference file for 'chardet' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 183. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-chardet-reference.md`
**Context:** The reference file for 'chardet' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 184. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-charls-reference.md`
**Context:** The reference file for 'charls' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 185. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-charls-reference.md`
**Context:** The reference file for 'charls' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 186. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-charls-reference.md`
**Context:** The reference file for 'charls' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 187. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-charset-normalizer-reference.md`
**Context:** The reference file for 'charset-normalizer' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 188. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-charset-normalizer-reference.md`
**Context:** The reference file for 'charset-normalizer' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 189. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-charset-normalizer-reference.md`
**Context:** The reference file for 'charset-normalizer' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 190. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-click-reference.md`
**Context:** The reference file for 'click' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 191. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-click-reference.md`
**Context:** The reference file for 'click' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 192. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-click-reference.md`
**Context:** The reference file for 'click' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 193. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-click-plugins-reference.md`
**Context:** The reference file for 'click-plugins' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 194. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-click-plugins-reference.md`
**Context:** The reference file for 'click-plugins' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 195. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-click-plugins-reference.md`
**Context:** The reference file for 'click-plugins' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 196. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-cligj-reference.md`
**Context:** The reference file for 'cligj' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 197. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-cligj-reference.md`
**Context:** The reference file for 'cligj' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 198. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-cligj-reference.md`
**Context:** The reference file for 'cligj' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 199. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-cloudpickle-reference.md`
**Context:** The reference file for 'cloudpickle' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 200. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-cloudpickle-reference.md`
**Context:** The reference file for 'cloudpickle' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 201. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-cloudpickle-reference.md`
**Context:** The reference file for 'cloudpickle' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 202. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-clyent-reference.md`
**Context:** The reference file for 'clyent' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 203. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-clyent-reference.md`
**Context:** The reference file for 'clyent' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 204. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-clyent-reference.md`
**Context:** The reference file for 'clyent' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 205. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-colorama-reference.md`
**Context:** The reference file for 'colorama' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 206. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-colorama-reference.md`
**Context:** The reference file for 'colorama' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 207. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-colorama-reference.md`
**Context:** The reference file for 'colorama' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 208. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-colorcet-reference.md`
**Context:** The reference file for 'colorcet' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 209. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-colorcet-reference.md`
**Context:** The reference file for 'colorcet' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 210. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-colorcet-reference.md`
**Context:** The reference file for 'colorcet' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 211. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-comtypes-reference.md`
**Context:** The reference file for 'comtypes' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 212. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-comtypes-reference.md`
**Context:** The reference file for 'comtypes' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 213. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-comtypes-reference.md`
**Context:** The reference file for 'comtypes' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 214. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-conda-reference.md`
**Context:** The reference file for 'conda' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 215. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-conda-reference.md`
**Context:** The reference file for 'conda' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 216. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-conda-reference.md`
**Context:** The reference file for 'conda' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 217. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-conda-build-reference.md`
**Context:** The reference file for 'conda-build' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 218. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-conda-build-reference.md`
**Context:** The reference file for 'conda-build' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 219. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-conda-build-reference.md`
**Context:** The reference file for 'conda-build' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 220. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-conda-content-trust-reference.md`
**Context:** The reference file for 'conda-content-trust' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 221. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-conda-content-trust-reference.md`
**Context:** The reference file for 'conda-content-trust' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 222. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-conda-content-trust-reference.md`
**Context:** The reference file for 'conda-content-trust' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 223. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-conda-env-reference.md`
**Context:** The reference file for 'conda-env' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 224. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-conda-env-reference.md`
**Context:** The reference file for 'conda-env' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 225. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-conda-env-reference.md`
**Context:** The reference file for 'conda-env' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 226. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-conda-pack-reference.md`
**Context:** The reference file for 'conda-pack' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 227. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-conda-pack-reference.md`
**Context:** The reference file for 'conda-pack' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 228. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-conda-pack-reference.md`
**Context:** The reference file for 'conda-pack' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 229. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-conda-package-handling-reference.md`
**Context:** The reference file for 'conda-package-handling' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 230. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-conda-package-handling-reference.md`
**Context:** The reference file for 'conda-package-handling' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 231. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-conda-package-handling-reference.md`
**Context:** The reference file for 'conda-package-handling' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 232. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-conda-package-streaming-reference.md`
**Context:** The reference file for 'conda-package-streaming' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 233. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-conda-package-streaming-reference.md`
**Context:** The reference file for 'conda-package-streaming' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 234. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-conda-package-streaming-reference.md`
**Context:** The reference file for 'conda-package-streaming' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 235. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-conda-repo-cli-reference.md`
**Context:** The reference file for 'conda-repo-cli' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 236. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-conda-repo-cli-reference.md`
**Context:** The reference file for 'conda-repo-cli' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 237. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-conda-repo-cli-reference.md`
**Context:** The reference file for 'conda-repo-cli' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 238. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-conda-token-reference.md`
**Context:** The reference file for 'conda-token' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 239. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-conda-token-reference.md`
**Context:** The reference file for 'conda-token' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 240. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-conda-token-reference.md`
**Context:** The reference file for 'conda-token' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 241. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-conda-verify-reference.md`
**Context:** The reference file for 'conda-verify' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 242. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-conda-verify-reference.md`
**Context:** The reference file for 'conda-verify' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 243. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-conda-verify-reference.md`
**Context:** The reference file for 'conda-verify' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 244. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-config-reference.md`
**Context:** The reference file for 'config' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 245. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-config-reference.md`
**Context:** The reference file for 'config' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 246. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-config-reference.md`
**Context:** The reference file for 'config' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 247. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-consoleshortcut-reference.md`
**Context:** The reference file for 'console_shortcut' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 248. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-consoleshortcut-reference.md`
**Context:** The reference file for 'console_shortcut' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 249. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-consoleshortcut-reference.md`
**Context:** The reference file for 'console_shortcut' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 250. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-constantly-reference.md`
**Context:** The reference file for 'constantly' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 251. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-constantly-reference.md`
**Context:** The reference file for 'constantly' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 252. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-constantly-reference.md`
**Context:** The reference file for 'constantly' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 253. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-cookiecutter-reference.md`
**Context:** The reference file for 'cookiecutter' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 254. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-cookiecutter-reference.md`
**Context:** The reference file for 'cookiecutter' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 255. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-cookiecutter-reference.md`
**Context:** The reference file for 'cookiecutter' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 256. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-cryptography-reference.md`
**Context:** The reference file for 'cryptography' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 257. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-cryptography-reference.md`
**Context:** The reference file for 'cryptography' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 258. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-cryptography-reference.md`
**Context:** The reference file for 'cryptography' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 259. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-cssselect-reference.md`
**Context:** The reference file for 'cssselect' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 260. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-cssselect-reference.md`
**Context:** The reference file for 'cssselect' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 261. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-cssselect-reference.md`
**Context:** The reference file for 'cssselect' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 262. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-curl-reference.md`
**Context:** The reference file for 'curl' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 263. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-curl-reference.md`
**Context:** The reference file for 'curl' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 264. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-curl-reference.md`
**Context:** The reference file for 'curl' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 265. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-cvxpy-reference.md`
**Context:** The reference file for 'cvxpy' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 266. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-cvxpy-reference.md`
**Context:** The reference file for 'cvxpy' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 267. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-cvxpy-reference.md`
**Context:** The reference file for 'cvxpy' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 268. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-cycler-reference.md`
**Context:** The reference file for 'cycler' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 269. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-cycler-reference.md`
**Context:** The reference file for 'cycler' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 270. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-cycler-reference.md`
**Context:** The reference file for 'cycler' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 271. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-cython-reference.md`
**Context:** The reference file for 'cython' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 272. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-cython-reference.md`
**Context:** The reference file for 'cython' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 273. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-cython-reference.md`
**Context:** The reference file for 'cython' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 274. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-cytoolz-reference.md`
**Context:** The reference file for 'cytoolz' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 275. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-cytoolz-reference.md`
**Context:** The reference file for 'cytoolz' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 276. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-cytoolz-reference.md`
**Context:** The reference file for 'cytoolz' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 277. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-daal4py-reference.md`
**Context:** The reference file for 'daal4py' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 278. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-daal4py-reference.md`
**Context:** The reference file for 'daal4py' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 279. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-daal4py-reference.md`
**Context:** The reference file for 'daal4py' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 280. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-dal-reference.md`
**Context:** The reference file for 'dal' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 281. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-dal-reference.md`
**Context:** The reference file for 'dal' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 282. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-dal-reference.md`
**Context:** The reference file for 'dal' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 283. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-dash-reference.md`
**Context:** The reference file for 'dash' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 284. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-dash-reference.md`
**Context:** The reference file for 'dash' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 285. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-dash-reference.md`
**Context:** The reference file for 'dash' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 286. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-dash-core-components-reference.md`
**Context:** The reference file for 'dash-core-components' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 287. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-dash-core-components-reference.md`
**Context:** The reference file for 'dash-core-components' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 288. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-dash-core-components-reference.md`
**Context:** The reference file for 'dash-core-components' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 289. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-dash-html-components-reference.md`
**Context:** The reference file for 'dash-html-components' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 290. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-dash-html-components-reference.md`
**Context:** The reference file for 'dash-html-components' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 291. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-dash-html-components-reference.md`
**Context:** The reference file for 'dash-html-components' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 292. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-dash-table-reference.md`
**Context:** The reference file for 'dash-table' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 293. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-dash-table-reference.md`
**Context:** The reference file for 'dash-table' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 294. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-dash-table-reference.md`
**Context:** The reference file for 'dash-table' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 295. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-dask-reference.md`
**Context:** The reference file for 'dask' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 296. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-dask-reference.md`
**Context:** The reference file for 'dask' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 297. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-dask-reference.md`
**Context:** The reference file for 'dask' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 298. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-dask-core-reference.md`
**Context:** The reference file for 'dask-core' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 299. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-dask-core-reference.md`
**Context:** The reference file for 'dask-core' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 300. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-dask-core-reference.md`
**Context:** The reference file for 'dask-core' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 301. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-dataclasses-reference.md`
**Context:** The reference file for 'dataclasses' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 302. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-dataclasses-reference.md`
**Context:** The reference file for 'dataclasses' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 303. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-dataclasses-reference.md`
**Context:** The reference file for 'dataclasses' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 304. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-datashader-reference.md`
**Context:** The reference file for 'datashader' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 305. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-datashader-reference.md`
**Context:** The reference file for 'datashader' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 306. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-datashader-reference.md`
**Context:** The reference file for 'datashader' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 307. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-datashape-reference.md`
**Context:** The reference file for 'datashape' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 308. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-datashape-reference.md`
**Context:** The reference file for 'datashape' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 309. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-datashape-reference.md`
**Context:** The reference file for 'datashape' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 310. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-debugpy-reference.md`
**Context:** The reference file for 'debugpy' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 311. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-debugpy-reference.md`
**Context:** The reference file for 'debugpy' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 312. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-debugpy-reference.md`
**Context:** The reference file for 'debugpy' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 313. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-decorator-reference.md`
**Context:** The reference file for 'decorator' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 314. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-decorator-reference.md`
**Context:** The reference file for 'decorator' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 315. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-decorator-reference.md`
**Context:** The reference file for 'decorator' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 316. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-defusedxml-reference.md`
**Context:** The reference file for 'defusedxml' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 317. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-defusedxml-reference.md`
**Context:** The reference file for 'defusedxml' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 318. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-defusedxml-reference.md`
**Context:** The reference file for 'defusedxml' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 319. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-diff-match-patch-reference.md`
**Context:** The reference file for 'diff-match-patch' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 320. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-diff-match-patch-reference.md`
**Context:** The reference file for 'diff-match-patch' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 321. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-diff-match-patch-reference.md`
**Context:** The reference file for 'diff-match-patch' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 322. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-dill-reference.md`
**Context:** The reference file for 'dill' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 323. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-dill-reference.md`
**Context:** The reference file for 'dill' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 324. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-dill-reference.md`
**Context:** The reference file for 'dill' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 325. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-distributed-reference.md`
**Context:** The reference file for 'distributed' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 326. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-distributed-reference.md`
**Context:** The reference file for 'distributed' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 327. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-distributed-reference.md`
**Context:** The reference file for 'distributed' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 328. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-docutils-reference.md`
**Context:** The reference file for 'docutils' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 329. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-docutils-reference.md`
**Context:** The reference file for 'docutils' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 330. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-docutils-reference.md`
**Context:** The reference file for 'docutils' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 331. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-ecos-reference.md`
**Context:** The reference file for 'ecos' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 332. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-ecos-reference.md`
**Context:** The reference file for 'ecos' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 333. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-ecos-reference.md`
**Context:** The reference file for 'ecos' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 334. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-entrypoints-reference.md`
**Context:** The reference file for 'entrypoints' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 335. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-entrypoints-reference.md`
**Context:** The reference file for 'entrypoints' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 336. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-entrypoints-reference.md`
**Context:** The reference file for 'entrypoints' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 337. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-etxmlfile-reference.md`
**Context:** The reference file for 'et_xmlfile' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 338. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-etxmlfile-reference.md`
**Context:** The reference file for 'et_xmlfile' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 339. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-etxmlfile-reference.md`
**Context:** The reference file for 'et_xmlfile' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 340. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-expat-reference.md`
**Context:** The reference file for 'expat' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 341. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-expat-reference.md`
**Context:** The reference file for 'expat' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 342. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-expat-reference.md`
**Context:** The reference file for 'expat' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 343. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-ffn-reference.md`
**Context:** The reference file for 'ffn' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 344. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-ffn-reference.md`
**Context:** The reference file for 'ffn' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 345. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-ffn-reference.md`
**Context:** The reference file for 'ffn' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 346. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-fftw-reference.md`
**Context:** The reference file for 'fftw' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 347. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-fftw-reference.md`
**Context:** The reference file for 'fftw' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 348. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-fftw-reference.md`
**Context:** The reference file for 'fftw' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 349. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-filelock-reference.md`
**Context:** The reference file for 'filelock' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 350. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-filelock-reference.md`
**Context:** The reference file for 'filelock' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 351. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-filelock-reference.md`
**Context:** The reference file for 'filelock' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 352. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-fiona-reference.md`
**Context:** The reference file for 'fiona' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 353. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-fiona-reference.md`
**Context:** The reference file for 'fiona' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 354. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-fiona-reference.md`
**Context:** The reference file for 'fiona' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 355. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-flake8-reference.md`
**Context:** The reference file for 'flake8' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 356. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-flake8-reference.md`
**Context:** The reference file for 'flake8' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 357. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-flake8-reference.md`
**Context:** The reference file for 'flake8' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 358. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-flask-reference.md`
**Context:** The reference file for 'flask' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 359. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-flask-reference.md`
**Context:** The reference file for 'flask' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 360. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-flask-reference.md`
**Context:** The reference file for 'flask' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 361. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-folium-reference.md`
**Context:** The reference file for 'folium' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 362. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-folium-reference.md`
**Context:** The reference file for 'folium' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 363. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-folium-reference.md`
**Context:** The reference file for 'folium' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 364. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-fonttools-reference.md`
**Context:** The reference file for 'fonttools' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 365. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-fonttools-reference.md`
**Context:** The reference file for 'fonttools' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 366. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-fonttools-reference.md`
**Context:** The reference file for 'fonttools' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 367. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-freetype-reference.md`
**Context:** The reference file for 'freetype' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 368. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-freetype-reference.md`
**Context:** The reference file for 'freetype' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 369. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-freetype-reference.md`
**Context:** The reference file for 'freetype' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 370. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-freexl-reference.md`
**Context:** The reference file for 'freexl' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 371. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-freexl-reference.md`
**Context:** The reference file for 'freexl' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 372. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-freexl-reference.md`
**Context:** The reference file for 'freexl' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 373. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-frozendict-reference.md`
**Context:** The reference file for 'frozendict' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 374. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-frozendict-reference.md`
**Context:** The reference file for 'frozendict' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 375. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-frozendict-reference.md`
**Context:** The reference file for 'frozendict' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 376. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-fsspec-reference.md`
**Context:** The reference file for 'fsspec' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 377. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-fsspec-reference.md`
**Context:** The reference file for 'fsspec' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 378. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-fsspec-reference.md`
**Context:** The reference file for 'fsspec' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 379. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-future-reference.md`
**Context:** The reference file for 'future' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 380. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-future-reference.md`
**Context:** The reference file for 'future' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 381. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-future-reference.md`
**Context:** The reference file for 'future' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 382. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-gdal-reference.md`
**Context:** The reference file for 'gdal' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 383. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-gdal-reference.md`
**Context:** The reference file for 'gdal' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 384. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-gdal-reference.md`
**Context:** The reference file for 'gdal' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 385. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-gensim-reference.md`
**Context:** The reference file for 'gensim' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 386. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-gensim-reference.md`
**Context:** The reference file for 'gensim' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 387. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-gensim-reference.md`
**Context:** The reference file for 'gensim' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 388. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-geographiclib-reference.md`
**Context:** The reference file for 'geographiclib' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 389. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-geographiclib-reference.md`
**Context:** The reference file for 'geographiclib' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 390. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-geographiclib-reference.md`
**Context:** The reference file for 'geographiclib' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 391. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-geopandas-reference.md`
**Context:** The reference file for 'geopandas' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 392. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-geopandas-reference.md`
**Context:** The reference file for 'geopandas' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 393. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-geopandas-reference.md`
**Context:** The reference file for 'geopandas' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 394. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-geopandas-base-reference.md`
**Context:** The reference file for 'geopandas-base' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 395. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-geopandas-base-reference.md`
**Context:** The reference file for 'geopandas-base' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 396. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-geopandas-base-reference.md`
**Context:** The reference file for 'geopandas-base' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 397. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-geopy-reference.md`
**Context:** The reference file for 'geopy' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 398. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-geopy-reference.md`
**Context:** The reference file for 'geopy' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 399. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-geopy-reference.md`
**Context:** The reference file for 'geopy' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 400. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-geos-reference.md`
**Context:** The reference file for 'geos' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 401. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-geos-reference.md`
**Context:** The reference file for 'geos' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 402. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-geos-reference.md`
**Context:** The reference file for 'geos' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 403. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-geotiff-reference.md`
**Context:** The reference file for 'geotiff' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 404. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-geotiff-reference.md`
**Context:** The reference file for 'geotiff' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 405. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-geotiff-reference.md`
**Context:** The reference file for 'geotiff' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 406. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-gflags-reference.md`
**Context:** The reference file for 'gflags' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 407. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-gflags-reference.md`
**Context:** The reference file for 'gflags' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 408. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-gflags-reference.md`
**Context:** The reference file for 'gflags' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 409. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-giflib-reference.md`
**Context:** The reference file for 'giflib' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 410. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-giflib-reference.md`
**Context:** The reference file for 'giflib' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 411. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-giflib-reference.md`
**Context:** The reference file for 'giflib' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 412. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-glob2-reference.md`
**Context:** The reference file for 'glob2' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 413. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-glob2-reference.md`
**Context:** The reference file for 'glob2' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 414. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-glob2-reference.md`
**Context:** The reference file for 'glob2' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 415. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-glog-reference.md`
**Context:** The reference file for 'glog' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 416. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-glog-reference.md`
**Context:** The reference file for 'glog' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 417. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-glog-reference.md`
**Context:** The reference file for 'glog' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 418. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-greenlet-reference.md`
**Context:** The reference file for 'greenlet' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 419. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-greenlet-reference.md`
**Context:** The reference file for 'greenlet' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 420. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-greenlet-reference.md`
**Context:** The reference file for 'greenlet' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 421. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-h5py-reference.md`
**Context:** The reference file for 'h5py' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 422. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-h5py-reference.md`
**Context:** The reference file for 'h5py' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 423. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-h5py-reference.md`
**Context:** The reference file for 'h5py' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 424. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-hdf4-reference.md`
**Context:** The reference file for 'hdf4' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 425. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-hdf4-reference.md`
**Context:** The reference file for 'hdf4' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 426. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-hdf4-reference.md`
**Context:** The reference file for 'hdf4' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 427. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-hdf5-reference.md`
**Context:** The reference file for 'hdf5' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 428. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-hdf5-reference.md`
**Context:** The reference file for 'hdf5' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 429. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-hdf5-reference.md`
**Context:** The reference file for 'hdf5' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 430. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-heapdict-reference.md`
**Context:** The reference file for 'heapdict' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 431. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-heapdict-reference.md`
**Context:** The reference file for 'heapdict' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 432. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-heapdict-reference.md`
**Context:** The reference file for 'heapdict' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 433. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-holoviews-reference.md`
**Context:** The reference file for 'holoviews' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 434. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-holoviews-reference.md`
**Context:** The reference file for 'holoviews' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 435. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-holoviews-reference.md`
**Context:** The reference file for 'holoviews' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 436. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-html5lib-reference.md`
**Context:** The reference file for 'html5lib' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 437. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-html5lib-reference.md`
**Context:** The reference file for 'html5lib' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 438. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-html5lib-reference.md`
**Context:** The reference file for 'html5lib' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 439. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-hvplot-reference.md`
**Context:** The reference file for 'hvplot' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 440. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-hvplot-reference.md`
**Context:** The reference file for 'hvplot' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 441. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-hvplot-reference.md`
**Context:** The reference file for 'hvplot' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 442. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-hyperlink-reference.md`
**Context:** The reference file for 'hyperlink' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 443. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-hyperlink-reference.md`
**Context:** The reference file for 'hyperlink' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 444. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-hyperlink-reference.md`
**Context:** The reference file for 'hyperlink' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 445. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-iccrt-reference.md`
**Context:** The reference file for 'icc_rt' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 446. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-iccrt-reference.md`
**Context:** The reference file for 'icc_rt' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 447. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-iccrt-reference.md`
**Context:** The reference file for 'icc_rt' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 448. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-icu-reference.md`
**Context:** The reference file for 'icu' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 449. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-icu-reference.md`
**Context:** The reference file for 'icu' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 450. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-icu-reference.md`
**Context:** The reference file for 'icu' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 451. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-idna-reference.md`
**Context:** The reference file for 'idna' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 452. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-idna-reference.md`
**Context:** The reference file for 'idna' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 453. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-idna-reference.md`
**Context:** The reference file for 'idna' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 454. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-imagecodecs-reference.md`
**Context:** The reference file for 'imagecodecs' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 455. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-imagecodecs-reference.md`
**Context:** The reference file for 'imagecodecs' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 456. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-imagecodecs-reference.md`
**Context:** The reference file for 'imagecodecs' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 457. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-imageio-reference.md`
**Context:** The reference file for 'imageio' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 458. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-imageio-reference.md`
**Context:** The reference file for 'imageio' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 459. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-imageio-reference.md`
**Context:** The reference file for 'imageio' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 460. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-imagesize-reference.md`
**Context:** The reference file for 'imagesize' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 461. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-imagesize-reference.md`
**Context:** The reference file for 'imagesize' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 462. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-imagesize-reference.md`
**Context:** The reference file for 'imagesize' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 463. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-importlib-metadata-reference.md`
**Context:** The reference file for 'importlib-metadata' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 464. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-importlib-metadata-reference.md`
**Context:** The reference file for 'importlib-metadata' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 465. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-importlib-metadata-reference.md`
**Context:** The reference file for 'importlib-metadata' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 466. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-importlibmetadata-reference.md`
**Context:** The reference file for 'importlib_metadata' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 467. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-importlibmetadata-reference.md`
**Context:** The reference file for 'importlib_metadata' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 468. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-importlibmetadata-reference.md`
**Context:** The reference file for 'importlib_metadata' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 469. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-incremental-reference.md`
**Context:** The reference file for 'incremental' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 470. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-incremental-reference.md`
**Context:** The reference file for 'incremental' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 471. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-incremental-reference.md`
**Context:** The reference file for 'incremental' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 472. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-inflection-reference.md`
**Context:** The reference file for 'inflection' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 473. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-inflection-reference.md`
**Context:** The reference file for 'inflection' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 474. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-inflection-reference.md`
**Context:** The reference file for 'inflection' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 475. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-iniconfig-reference.md`
**Context:** The reference file for 'iniconfig' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 476. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-iniconfig-reference.md`
**Context:** The reference file for 'iniconfig' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 477. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-iniconfig-reference.md`
**Context:** The reference file for 'iniconfig' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 478. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-intake-reference.md`
**Context:** The reference file for 'intake' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 479. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-intake-reference.md`
**Context:** The reference file for 'intake' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 480. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-intake-reference.md`
**Context:** The reference file for 'intake' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 481. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-intel-openmp-reference.md`
**Context:** The reference file for 'intel-openmp' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 482. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-intel-openmp-reference.md`
**Context:** The reference file for 'intel-openmp' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 483. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-intel-openmp-reference.md`
**Context:** The reference file for 'intel-openmp' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 484. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-intervaltree-reference.md`
**Context:** The reference file for 'intervaltree' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 485. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-intervaltree-reference.md`
**Context:** The reference file for 'intervaltree' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 486. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-intervaltree-reference.md`
**Context:** The reference file for 'intervaltree' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 487. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-ipykernel-reference.md`
**Context:** The reference file for 'ipykernel' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 488. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-ipykernel-reference.md`
**Context:** The reference file for 'ipykernel' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 489. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-ipykernel-reference.md`
**Context:** The reference file for 'ipykernel' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 490. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-ipython-reference.md`
**Context:** The reference file for 'ipython' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 491. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-ipython-reference.md`
**Context:** The reference file for 'ipython' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 492. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-ipython-reference.md`
**Context:** The reference file for 'ipython' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 493. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-ipythongenutils-reference.md`
**Context:** The reference file for 'ipython_genutils' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 494. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-ipythongenutils-reference.md`
**Context:** The reference file for 'ipython_genutils' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 495. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-ipythongenutils-reference.md`
**Context:** The reference file for 'ipython_genutils' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 496. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-ipywidgets-reference.md`
**Context:** The reference file for 'ipywidgets' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 497. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-ipywidgets-reference.md`
**Context:** The reference file for 'ipywidgets' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 498. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-ipywidgets-reference.md`
**Context:** The reference file for 'ipywidgets' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 499. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-isort-reference.md`
**Context:** The reference file for 'isort' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 500. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-isort-reference.md`
**Context:** The reference file for 'isort' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 501. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-isort-reference.md`
**Context:** The reference file for 'isort' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 502. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-itemadapter-reference.md`
**Context:** The reference file for 'itemadapter' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 503. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-itemadapter-reference.md`
**Context:** The reference file for 'itemadapter' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 504. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-itemadapter-reference.md`
**Context:** The reference file for 'itemadapter' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 505. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-itemloaders-reference.md`
**Context:** The reference file for 'itemloaders' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 506. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-itemloaders-reference.md`
**Context:** The reference file for 'itemloaders' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 507. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-itemloaders-reference.md`
**Context:** The reference file for 'itemloaders' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 508. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-itsdangerous-reference.md`
**Context:** The reference file for 'itsdangerous' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 509. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-itsdangerous-reference.md`
**Context:** The reference file for 'itsdangerous' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 510. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-itsdangerous-reference.md`
**Context:** The reference file for 'itsdangerous' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 511. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-jdcal-reference.md`
**Context:** The reference file for 'jdcal' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 512. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-jdcal-reference.md`
**Context:** The reference file for 'jdcal' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 513. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-jdcal-reference.md`
**Context:** The reference file for 'jdcal' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 514. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-jedi-reference.md`
**Context:** The reference file for 'jedi' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 515. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-jedi-reference.md`
**Context:** The reference file for 'jedi' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 516. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-jedi-reference.md`
**Context:** The reference file for 'jedi' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 517. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-jellyfish-reference.md`
**Context:** The reference file for 'jellyfish' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 518. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-jellyfish-reference.md`
**Context:** The reference file for 'jellyfish' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 519. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-jellyfish-reference.md`
**Context:** The reference file for 'jellyfish' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 520. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-jinja2-reference.md`
**Context:** The reference file for 'jinja2' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 521. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-jinja2-reference.md`
**Context:** The reference file for 'jinja2' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 522. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-jinja2-reference.md`
**Context:** The reference file for 'jinja2' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 523. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-jinja2-time-reference.md`
**Context:** The reference file for 'jinja2-time' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 524. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-jinja2-time-reference.md`
**Context:** The reference file for 'jinja2-time' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 525. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-jinja2-time-reference.md`
**Context:** The reference file for 'jinja2-time' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 526. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-jmespath-reference.md`
**Context:** The reference file for 'jmespath' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 527. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-jmespath-reference.md`
**Context:** The reference file for 'jmespath' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 528. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-jmespath-reference.md`
**Context:** The reference file for 'jmespath' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 529. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-joblib-reference.md`
**Context:** The reference file for 'joblib' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 530. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-joblib-reference.md`
**Context:** The reference file for 'joblib' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 531. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-joblib-reference.md`
**Context:** The reference file for 'joblib' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 532. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-jpeg-reference.md`
**Context:** The reference file for 'jpeg' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 533. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-jpeg-reference.md`
**Context:** The reference file for 'jpeg' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 534. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-jpeg-reference.md`
**Context:** The reference file for 'jpeg' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 535. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-jq-reference.md`
**Context:** The reference file for 'jq' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 536. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-jq-reference.md`
**Context:** The reference file for 'jq' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 537. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-jq-reference.md`
**Context:** The reference file for 'jq' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 538. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-json5-reference.md`
**Context:** The reference file for 'json5' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 539. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-json5-reference.md`
**Context:** The reference file for 'json5' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 540. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-json5-reference.md`
**Context:** The reference file for 'json5' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 541. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-jsonpatch-reference.md`
**Context:** The reference file for 'jsonpatch' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 542. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-jsonpatch-reference.md`
**Context:** The reference file for 'jsonpatch' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 543. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-jsonpatch-reference.md`
**Context:** The reference file for 'jsonpatch' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 544. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-jsonpointer-reference.md`
**Context:** The reference file for 'jsonpointer' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 545. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-jsonpointer-reference.md`
**Context:** The reference file for 'jsonpointer' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 546. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-jsonpointer-reference.md`
**Context:** The reference file for 'jsonpointer' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 547. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-jsonschema-reference.md`
**Context:** The reference file for 'jsonschema' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 548. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-jsonschema-reference.md`
**Context:** The reference file for 'jsonschema' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 549. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-jsonschema-reference.md`
**Context:** The reference file for 'jsonschema' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 550. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-jupyterclient-reference.md`
**Context:** The reference file for 'jupyter_client' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 551. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-jupyterclient-reference.md`
**Context:** The reference file for 'jupyter_client' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 552. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-jupyterclient-reference.md`
**Context:** The reference file for 'jupyter_client' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 553. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-jupyterconsole-reference.md`
**Context:** The reference file for 'jupyter_console' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 554. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-jupyterconsole-reference.md`
**Context:** The reference file for 'jupyter_console' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 555. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-jupyterconsole-reference.md`
**Context:** The reference file for 'jupyter_console' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 556. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-jupytercore-reference.md`
**Context:** The reference file for 'jupyter_core' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 557. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-jupytercore-reference.md`
**Context:** The reference file for 'jupyter_core' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 558. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-jupytercore-reference.md`
**Context:** The reference file for 'jupyter_core' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 559. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-jupyterserver-reference.md`
**Context:** The reference file for 'jupyter_server' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 560. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-jupyterserver-reference.md`
**Context:** The reference file for 'jupyter_server' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 561. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-jupyterserver-reference.md`
**Context:** The reference file for 'jupyter_server' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 562. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-jupyterlab-reference.md`
**Context:** The reference file for 'jupyterlab' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 563. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-jupyterlab-reference.md`
**Context:** The reference file for 'jupyterlab' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 564. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-jupyterlab-reference.md`
**Context:** The reference file for 'jupyterlab' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 565. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-jupyterlabpygments-reference.md`
**Context:** The reference file for 'jupyterlab_pygments' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 566. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-jupyterlabpygments-reference.md`
**Context:** The reference file for 'jupyterlab_pygments' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 567. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-jupyterlabpygments-reference.md`
**Context:** The reference file for 'jupyterlab_pygments' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 568. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-jupyterlabserver-reference.md`
**Context:** The reference file for 'jupyterlab_server' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 569. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-jupyterlabserver-reference.md`
**Context:** The reference file for 'jupyterlab_server' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 570. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-jupyterlabserver-reference.md`
**Context:** The reference file for 'jupyterlab_server' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 571. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-jupyterlabwidgets-reference.md`
**Context:** The reference file for 'jupyterlab_widgets' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 572. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-jupyterlabwidgets-reference.md`
**Context:** The reference file for 'jupyterlab_widgets' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 573. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-jupyterlabwidgets-reference.md`
**Context:** The reference file for 'jupyterlab_widgets' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 574. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-kealib-reference.md`
**Context:** The reference file for 'kealib' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 575. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-kealib-reference.md`
**Context:** The reference file for 'kealib' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 576. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-kealib-reference.md`
**Context:** The reference file for 'kealib' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 577. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-keyring-reference.md`
**Context:** The reference file for 'keyring' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 578. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-keyring-reference.md`
**Context:** The reference file for 'keyring' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 579. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-keyring-reference.md`
**Context:** The reference file for 'keyring' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 580. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-kiwisolver-reference.md`
**Context:** The reference file for 'kiwisolver' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 581. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-kiwisolver-reference.md`
**Context:** The reference file for 'kiwisolver' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 582. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-kiwisolver-reference.md`
**Context:** The reference file for 'kiwisolver' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 583. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-krb5-reference.md`
**Context:** The reference file for 'krb5' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 584. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-krb5-reference.md`
**Context:** The reference file for 'krb5' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 585. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-krb5-reference.md`
**Context:** The reference file for 'krb5' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 586. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-lazy-object-proxy-reference.md`
**Context:** The reference file for 'lazy-object-proxy' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 587. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-lazy-object-proxy-reference.md`
**Context:** The reference file for 'lazy-object-proxy' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 588. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-lazy-object-proxy-reference.md`
**Context:** The reference file for 'lazy-object-proxy' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 589. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-lcms2-reference.md`
**Context:** The reference file for 'lcms2' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 590. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-lcms2-reference.md`
**Context:** The reference file for 'lcms2' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 591. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-lcms2-reference.md`
**Context:** The reference file for 'lcms2' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 592. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-lerc-reference.md`
**Context:** The reference file for 'lerc' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 593. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-lerc-reference.md`
**Context:** The reference file for 'lerc' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 594. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-lerc-reference.md`
**Context:** The reference file for 'lerc' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 595. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-libaec-reference.md`
**Context:** The reference file for 'libaec' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 596. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-libaec-reference.md`
**Context:** The reference file for 'libaec' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 597. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-libaec-reference.md`
**Context:** The reference file for 'libaec' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 598. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-libarchive-reference.md`
**Context:** The reference file for 'libarchive' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 599. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-libarchive-reference.md`
**Context:** The reference file for 'libarchive' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 600. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-libarchive-reference.md`
**Context:** The reference file for 'libarchive' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 601. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-libboost-reference.md`
**Context:** The reference file for 'libboost' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 602. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-libboost-reference.md`
**Context:** The reference file for 'libboost' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 603. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-libboost-reference.md`
**Context:** The reference file for 'libboost' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 604. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-libbrotlicommon-reference.md`
**Context:** The reference file for 'libbrotlicommon' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 605. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-libbrotlicommon-reference.md`
**Context:** The reference file for 'libbrotlicommon' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 606. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-libbrotlicommon-reference.md`
**Context:** The reference file for 'libbrotlicommon' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 607. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-libbrotlidec-reference.md`
**Context:** The reference file for 'libbrotlidec' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 608. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-libbrotlidec-reference.md`
**Context:** The reference file for 'libbrotlidec' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 609. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-libbrotlidec-reference.md`
**Context:** The reference file for 'libbrotlidec' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 610. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-libbrotlienc-reference.md`
**Context:** The reference file for 'libbrotlienc' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 611. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-libbrotlienc-reference.md`
**Context:** The reference file for 'libbrotlienc' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 612. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-libbrotlienc-reference.md`
**Context:** The reference file for 'libbrotlienc' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 613. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-libcurl-reference.md`
**Context:** The reference file for 'libcurl' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 614. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-libcurl-reference.md`
**Context:** The reference file for 'libcurl' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 615. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-libcurl-reference.md`
**Context:** The reference file for 'libcurl' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 616. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-libdeflate-reference.md`
**Context:** The reference file for 'libdeflate' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 617. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-libdeflate-reference.md`
**Context:** The reference file for 'libdeflate' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 618. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-libdeflate-reference.md`
**Context:** The reference file for 'libdeflate' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 619. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-libexpat-reference.md`
**Context:** The reference file for 'libexpat' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 620. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-libexpat-reference.md`
**Context:** The reference file for 'libexpat' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 621. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-libexpat-reference.md`
**Context:** The reference file for 'libexpat' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 622. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-libgdal-reference.md`
**Context:** The reference file for 'libgdal' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 623. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-libgdal-reference.md`
**Context:** The reference file for 'libgdal' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 624. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-libgdal-reference.md`
**Context:** The reference file for 'libgdal' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 625. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-libiconv-reference.md`
**Context:** The reference file for 'libiconv' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 626. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-libiconv-reference.md`
**Context:** The reference file for 'libiconv' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 627. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-libiconv-reference.md`
**Context:** The reference file for 'libiconv' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 628. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-liblief-reference.md`
**Context:** The reference file for 'liblief' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 629. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-liblief-reference.md`
**Context:** The reference file for 'liblief' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 630. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-liblief-reference.md`
**Context:** The reference file for 'liblief' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 631. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-libnetcdf-reference.md`
**Context:** The reference file for 'libnetcdf' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 632. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-libnetcdf-reference.md`
**Context:** The reference file for 'libnetcdf' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 633. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-libnetcdf-reference.md`
**Context:** The reference file for 'libnetcdf' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 634. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-libpng-reference.md`
**Context:** The reference file for 'libpng' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 635. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-libpng-reference.md`
**Context:** The reference file for 'libpng' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 636. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-libpng-reference.md`
**Context:** The reference file for 'libpng' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 637. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-libpq-reference.md`
**Context:** The reference file for 'libpq' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 638. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-libpq-reference.md`
**Context:** The reference file for 'libpq' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 639. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-libpq-reference.md`
**Context:** The reference file for 'libpq' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 640. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-libprotobuf-reference.md`
**Context:** The reference file for 'libprotobuf' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 641. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-libprotobuf-reference.md`
**Context:** The reference file for 'libprotobuf' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 642. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-libprotobuf-reference.md`
**Context:** The reference file for 'libprotobuf' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 643. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-libsodium-reference.md`
**Context:** The reference file for 'libsodium' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 644. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-libsodium-reference.md`
**Context:** The reference file for 'libsodium' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 645. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-libsodium-reference.md`
**Context:** The reference file for 'libsodium' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 646. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-libspatialindex-reference.md`
**Context:** The reference file for 'libspatialindex' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 647. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-libspatialindex-reference.md`
**Context:** The reference file for 'libspatialindex' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 648. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-libspatialindex-reference.md`
**Context:** The reference file for 'libspatialindex' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 649. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-libspatialite-reference.md`
**Context:** The reference file for 'libspatialite' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 650. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-libspatialite-reference.md`
**Context:** The reference file for 'libspatialite' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 651. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-libspatialite-reference.md`
**Context:** The reference file for 'libspatialite' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 652. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-libssh2-reference.md`
**Context:** The reference file for 'libssh2' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 653. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-libssh2-reference.md`
**Context:** The reference file for 'libssh2' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 654. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-libssh2-reference.md`
**Context:** The reference file for 'libssh2' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 655. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-libthrift-reference.md`
**Context:** The reference file for 'libthrift' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 656. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-libthrift-reference.md`
**Context:** The reference file for 'libthrift' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 657. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-libthrift-reference.md`
**Context:** The reference file for 'libthrift' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 658. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-libtiff-reference.md`
**Context:** The reference file for 'libtiff' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 659. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-libtiff-reference.md`
**Context:** The reference file for 'libtiff' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 660. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-libtiff-reference.md`
**Context:** The reference file for 'libtiff' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 661. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-libwebp-reference.md`
**Context:** The reference file for 'libwebp' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 662. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-libwebp-reference.md`
**Context:** The reference file for 'libwebp' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 663. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-libwebp-reference.md`
**Context:** The reference file for 'libwebp' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 664. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-libwebp-base-reference.md`
**Context:** The reference file for 'libwebp-base' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 665. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-libwebp-base-reference.md`
**Context:** The reference file for 'libwebp-base' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 666. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-libwebp-base-reference.md`
**Context:** The reference file for 'libwebp-base' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 667. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-libxml2-reference.md`
**Context:** The reference file for 'libxml2' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 668. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-libxml2-reference.md`
**Context:** The reference file for 'libxml2' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 669. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-libxml2-reference.md`
**Context:** The reference file for 'libxml2' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 670. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-libxslt-reference.md`
**Context:** The reference file for 'libxslt' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 671. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-libxslt-reference.md`
**Context:** The reference file for 'libxslt' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 672. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-libxslt-reference.md`
**Context:** The reference file for 'libxslt' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 673. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-libzip-reference.md`
**Context:** The reference file for 'libzip' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 674. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-libzip-reference.md`
**Context:** The reference file for 'libzip' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 675. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-libzip-reference.md`
**Context:** The reference file for 'libzip' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 676. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-libzopfli-reference.md`
**Context:** The reference file for 'libzopfli' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 677. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-libzopfli-reference.md`
**Context:** The reference file for 'libzopfli' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 678. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-libzopfli-reference.md`
**Context:** The reference file for 'libzopfli' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 679. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-llvmlite-reference.md`
**Context:** The reference file for 'llvmlite' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 680. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-llvmlite-reference.md`
**Context:** The reference file for 'llvmlite' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 681. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-llvmlite-reference.md`
**Context:** The reference file for 'llvmlite' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 682. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-locket-reference.md`
**Context:** The reference file for 'locket' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 683. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-locket-reference.md`
**Context:** The reference file for 'locket' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 684. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-locket-reference.md`
**Context:** The reference file for 'locket' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 685. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-lxml-reference.md`
**Context:** The reference file for 'lxml' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 686. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-lxml-reference.md`
**Context:** The reference file for 'lxml' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 687. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-lxml-reference.md`
**Context:** The reference file for 'lxml' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 688. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-lz4-reference.md`
**Context:** The reference file for 'lz4' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 689. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-lz4-reference.md`
**Context:** The reference file for 'lz4' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 690. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-lz4-reference.md`
**Context:** The reference file for 'lz4' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 691. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-lz4-c-reference.md`
**Context:** The reference file for 'lz4-c' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 692. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-lz4-c-reference.md`
**Context:** The reference file for 'lz4-c' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 693. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-lz4-c-reference.md`
**Context:** The reference file for 'lz4-c' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 694. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-lzo-reference.md`
**Context:** The reference file for 'lzo' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 695. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-lzo-reference.md`
**Context:** The reference file for 'lzo' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 696. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-lzo-reference.md`
**Context:** The reference file for 'lzo' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 697. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-m2-msys2-runtime-reference.md`
**Context:** The reference file for 'm2-msys2-runtime' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 698. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-m2-msys2-runtime-reference.md`
**Context:** The reference file for 'm2-msys2-runtime' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 699. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-m2-msys2-runtime-reference.md`
**Context:** The reference file for 'm2-msys2-runtime' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 700. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-m2-patch-reference.md`
**Context:** The reference file for 'm2-patch' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 701. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-m2-patch-reference.md`
**Context:** The reference file for 'm2-patch' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 702. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-m2-patch-reference.md`
**Context:** The reference file for 'm2-patch' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 703. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-m2w64-expat-reference.md`
**Context:** The reference file for 'm2w64-expat' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 704. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-m2w64-expat-reference.md`
**Context:** The reference file for 'm2w64-expat' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 705. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-m2w64-expat-reference.md`
**Context:** The reference file for 'm2w64-expat' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 706. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-m2w64-gcc-libgfortran-reference.md`
**Context:** The reference file for 'm2w64-gcc-libgfortran' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 707. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-m2w64-gcc-libgfortran-reference.md`
**Context:** The reference file for 'm2w64-gcc-libgfortran' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 708. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-m2w64-gcc-libgfortran-reference.md`
**Context:** The reference file for 'm2w64-gcc-libgfortran' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 709. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-m2w64-gcc-libs-reference.md`
**Context:** The reference file for 'm2w64-gcc-libs' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 710. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-m2w64-gcc-libs-reference.md`
**Context:** The reference file for 'm2w64-gcc-libs' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 711. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-m2w64-gcc-libs-reference.md`
**Context:** The reference file for 'm2w64-gcc-libs' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 712. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-m2w64-gcc-libs-core-reference.md`
**Context:** The reference file for 'm2w64-gcc-libs-core' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 713. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-m2w64-gcc-libs-core-reference.md`
**Context:** The reference file for 'm2w64-gcc-libs-core' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 714. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-m2w64-gcc-libs-core-reference.md`
**Context:** The reference file for 'm2w64-gcc-libs-core' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 715. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-m2w64-gettext-reference.md`
**Context:** The reference file for 'm2w64-gettext' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 716. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-m2w64-gettext-reference.md`
**Context:** The reference file for 'm2w64-gettext' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 717. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-m2w64-gettext-reference.md`
**Context:** The reference file for 'm2w64-gettext' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 718. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-m2w64-gmp-reference.md`
**Context:** The reference file for 'm2w64-gmp' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 719. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-m2w64-gmp-reference.md`
**Context:** The reference file for 'm2w64-gmp' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 720. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-m2w64-gmp-reference.md`
**Context:** The reference file for 'm2w64-gmp' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 721. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-m2w64-libiconv-reference.md`
**Context:** The reference file for 'm2w64-libiconv' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 722. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-m2w64-libiconv-reference.md`
**Context:** The reference file for 'm2w64-libiconv' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 723. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-m2w64-libiconv-reference.md`
**Context:** The reference file for 'm2w64-libiconv' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 724. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-m2w64-libwinpthread-git-reference.md`
**Context:** The reference file for 'm2w64-libwinpthread-git' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 725. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-m2w64-libwinpthread-git-reference.md`
**Context:** The reference file for 'm2w64-libwinpthread-git' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 726. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-m2w64-libwinpthread-git-reference.md`
**Context:** The reference file for 'm2w64-libwinpthread-git' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 727. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-m2w64-xz-reference.md`
**Context:** The reference file for 'm2w64-xz' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 728. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-m2w64-xz-reference.md`
**Context:** The reference file for 'm2w64-xz' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 729. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-m2w64-xz-reference.md`
**Context:** The reference file for 'm2w64-xz' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 730. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-mapclassify-reference.md`
**Context:** The reference file for 'mapclassify' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 731. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-mapclassify-reference.md`
**Context:** The reference file for 'mapclassify' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 732. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-mapclassify-reference.md`
**Context:** The reference file for 'mapclassify' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 733. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-markdown-reference.md`
**Context:** The reference file for 'markdown' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 734. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-markdown-reference.md`
**Context:** The reference file for 'markdown' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 735. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-markdown-reference.md`
**Context:** The reference file for 'markdown' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 736. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-markupsafe-reference.md`
**Context:** The reference file for 'markupsafe' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 737. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-markupsafe-reference.md`
**Context:** The reference file for 'markupsafe' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 738. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-markupsafe-reference.md`
**Context:** The reference file for 'markupsafe' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 739. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-matplotlib-base-reference.md`
**Context:** The reference file for 'matplotlib-base' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 740. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-matplotlib-base-reference.md`
**Context:** The reference file for 'matplotlib-base' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 741. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-matplotlib-base-reference.md`
**Context:** The reference file for 'matplotlib-base' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 742. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-matplotlib-inline-reference.md`
**Context:** The reference file for 'matplotlib-inline' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 743. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-matplotlib-inline-reference.md`
**Context:** The reference file for 'matplotlib-inline' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 744. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-matplotlib-inline-reference.md`
**Context:** The reference file for 'matplotlib-inline' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 745. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-mccabe-reference.md`
**Context:** The reference file for 'mccabe' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 746. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-mccabe-reference.md`
**Context:** The reference file for 'mccabe' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 747. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-mccabe-reference.md`
**Context:** The reference file for 'mccabe' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 748. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-memoryprofiler-reference.md`
**Context:** The reference file for 'memory_profiler' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 749. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-memoryprofiler-reference.md`
**Context:** The reference file for 'memory_profiler' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 750. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-memoryprofiler-reference.md`
**Context:** The reference file for 'memory_profiler' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 751. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-menuinst-reference.md`
**Context:** The reference file for 'menuinst' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 752. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-menuinst-reference.md`
**Context:** The reference file for 'menuinst' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 753. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-menuinst-reference.md`
**Context:** The reference file for 'menuinst' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 754. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-mistune-reference.md`
**Context:** The reference file for 'mistune' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 755. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-mistune-reference.md`
**Context:** The reference file for 'mistune' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 756. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-mistune-reference.md`
**Context:** The reference file for 'mistune' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 757. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-mkl-reference.md`
**Context:** The reference file for 'mkl' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 758. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-mkl-reference.md`
**Context:** The reference file for 'mkl' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 759. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-mkl-reference.md`
**Context:** The reference file for 'mkl' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 760. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-mkl-service-reference.md`
**Context:** The reference file for 'mkl-service' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 761. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-mkl-service-reference.md`
**Context:** The reference file for 'mkl-service' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 762. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-mkl-service-reference.md`
**Context:** The reference file for 'mkl-service' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 763. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-mklfft-reference.md`
**Context:** The reference file for 'mkl_fft' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 764. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-mklfft-reference.md`
**Context:** The reference file for 'mkl_fft' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 765. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-mklfft-reference.md`
**Context:** The reference file for 'mkl_fft' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 766. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-mklrandom-reference.md`
**Context:** The reference file for 'mkl_random' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 767. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-mklrandom-reference.md`
**Context:** The reference file for 'mkl_random' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 768. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-mklrandom-reference.md`
**Context:** The reference file for 'mkl_random' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 769. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-mock-reference.md`
**Context:** The reference file for 'mock' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 770. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-mock-reference.md`
**Context:** The reference file for 'mock' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 771. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-mock-reference.md`
**Context:** The reference file for 'mock' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 772. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-mpmath-reference.md`
**Context:** The reference file for 'mpmath' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 773. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-mpmath-reference.md`
**Context:** The reference file for 'mpmath' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 774. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-mpmath-reference.md`
**Context:** The reference file for 'mpmath' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 775. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-msgpack-python-reference.md`
**Context:** The reference file for 'msgpack-python' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 776. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-msgpack-python-reference.md`
**Context:** The reference file for 'msgpack-python' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 777. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-msgpack-python-reference.md`
**Context:** The reference file for 'msgpack-python' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 778. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-msys2-conda-epoch-reference.md`
**Context:** The reference file for 'msys2-conda-epoch' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 779. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-msys2-conda-epoch-reference.md`
**Context:** The reference file for 'msys2-conda-epoch' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 780. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-msys2-conda-epoch-reference.md`
**Context:** The reference file for 'msys2-conda-epoch' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 781. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-multipledispatch-reference.md`
**Context:** The reference file for 'multipledispatch' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 782. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-multipledispatch-reference.md`
**Context:** The reference file for 'multipledispatch' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 783. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-multipledispatch-reference.md`
**Context:** The reference file for 'multipledispatch' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 784. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-multiprocess-reference.md`
**Context:** The reference file for 'multiprocess' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 785. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-multiprocess-reference.md`
**Context:** The reference file for 'multiprocess' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 786. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-multiprocess-reference.md`
**Context:** The reference file for 'multiprocess' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 787. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-multitasking-reference.md`
**Context:** The reference file for 'multitasking' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 788. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-multitasking-reference.md`
**Context:** The reference file for 'multitasking' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 789. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-multitasking-reference.md`
**Context:** The reference file for 'multitasking' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 790. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-munch-reference.md`
**Context:** The reference file for 'munch' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 791. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-munch-reference.md`
**Context:** The reference file for 'munch' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 792. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-munch-reference.md`
**Context:** The reference file for 'munch' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 793. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-munkres-reference.md`
**Context:** The reference file for 'munkres' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 794. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-munkres-reference.md`
**Context:** The reference file for 'munkres' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 795. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-munkres-reference.md`
**Context:** The reference file for 'munkres' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 796. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-mypyextensions-reference.md`
**Context:** The reference file for 'mypy_extensions' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 797. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-mypyextensions-reference.md`
**Context:** The reference file for 'mypy_extensions' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 798. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-mypyextensions-reference.md`
**Context:** The reference file for 'mypy_extensions' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 799. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-navigator-updater-reference.md`
**Context:** The reference file for 'navigator-updater' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 800. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-navigator-updater-reference.md`
**Context:** The reference file for 'navigator-updater' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 801. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-navigator-updater-reference.md`
**Context:** The reference file for 'navigator-updater' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 802. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-nbclassic-reference.md`
**Context:** The reference file for 'nbclassic' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 803. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-nbclassic-reference.md`
**Context:** The reference file for 'nbclassic' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 804. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-nbclassic-reference.md`
**Context:** The reference file for 'nbclassic' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 805. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-nbclient-reference.md`
**Context:** The reference file for 'nbclient' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 806. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-nbclient-reference.md`
**Context:** The reference file for 'nbclient' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 807. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-nbclient-reference.md`
**Context:** The reference file for 'nbclient' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 808. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-nbconvert-reference.md`
**Context:** The reference file for 'nbconvert' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 809. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-nbconvert-reference.md`
**Context:** The reference file for 'nbconvert' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 810. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-nbconvert-reference.md`
**Context:** The reference file for 'nbconvert' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 811. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-nbformat-reference.md`
**Context:** The reference file for 'nbformat' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 812. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-nbformat-reference.md`
**Context:** The reference file for 'nbformat' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 813. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-nbformat-reference.md`
**Context:** The reference file for 'nbformat' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 814. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-nest-asyncio-reference.md`
**Context:** The reference file for 'nest-asyncio' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 815. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-nest-asyncio-reference.md`
**Context:** The reference file for 'nest-asyncio' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 816. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-nest-asyncio-reference.md`
**Context:** The reference file for 'nest-asyncio' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 817. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-networkx-reference.md`
**Context:** The reference file for 'networkx' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 818. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-networkx-reference.md`
**Context:** The reference file for 'networkx' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 819. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-networkx-reference.md`
**Context:** The reference file for 'networkx' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 820. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-nltk-reference.md`
**Context:** The reference file for 'nltk' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 821. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-nltk-reference.md`
**Context:** The reference file for 'nltk' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 822. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-nltk-reference.md`
**Context:** The reference file for 'nltk' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 823. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-nose-reference.md`
**Context:** The reference file for 'nose' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 824. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-nose-reference.md`
**Context:** The reference file for 'nose' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 825. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-nose-reference.md`
**Context:** The reference file for 'nose' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 826. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-notebook-reference.md`
**Context:** The reference file for 'notebook' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 827. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-notebook-reference.md`
**Context:** The reference file for 'notebook' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 828. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-notebook-reference.md`
**Context:** The reference file for 'notebook' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 829. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-numba-reference.md`
**Context:** The reference file for 'numba' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 830. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-numba-reference.md`
**Context:** The reference file for 'numba' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 831. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-numba-reference.md`
**Context:** The reference file for 'numba' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 832. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-numdifftools-reference.md`
**Context:** The reference file for 'numdifftools' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 833. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-numdifftools-reference.md`
**Context:** The reference file for 'numdifftools' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 834. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-numdifftools-reference.md`
**Context:** The reference file for 'numdifftools' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 835. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-numexpr-reference.md`
**Context:** The reference file for 'numexpr' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 836. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-numexpr-reference.md`
**Context:** The reference file for 'numexpr' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 837. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-numexpr-reference.md`
**Context:** The reference file for 'numexpr' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 838. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-numpy-base-reference.md`
**Context:** The reference file for 'numpy-base' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 839. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-numpy-base-reference.md`
**Context:** The reference file for 'numpy-base' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 840. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-numpy-base-reference.md`
**Context:** The reference file for 'numpy-base' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 841. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-numpydoc-reference.md`
**Context:** The reference file for 'numpydoc' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 842. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-numpydoc-reference.md`
**Context:** The reference file for 'numpydoc' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 843. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-numpydoc-reference.md`
**Context:** The reference file for 'numpydoc' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 844. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-olefile-reference.md`
**Context:** The reference file for 'olefile' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 845. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-olefile-reference.md`
**Context:** The reference file for 'olefile' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 846. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-olefile-reference.md`
**Context:** The reference file for 'olefile' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 847. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-openjpeg-reference.md`
**Context:** The reference file for 'openjpeg' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 848. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-openjpeg-reference.md`
**Context:** The reference file for 'openjpeg' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 849. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-openjpeg-reference.md`
**Context:** The reference file for 'openjpeg' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 850. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-openpyxl-reference.md`
**Context:** The reference file for 'openpyxl' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 851. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-openpyxl-reference.md`
**Context:** The reference file for 'openpyxl' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 852. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-openpyxl-reference.md`
**Context:** The reference file for 'openpyxl' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 853. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-openssl-reference.md`
**Context:** The reference file for 'openssl' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 854. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-openssl-reference.md`
**Context:** The reference file for 'openssl' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 855. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-openssl-reference.md`
**Context:** The reference file for 'openssl' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 856. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-osqp-reference.md`
**Context:** The reference file for 'osqp' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 857. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-osqp-reference.md`
**Context:** The reference file for 'osqp' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 858. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-osqp-reference.md`
**Context:** The reference file for 'osqp' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 859. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-packaging-reference.md`
**Context:** The reference file for 'packaging' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 860. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-packaging-reference.md`
**Context:** The reference file for 'packaging' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 861. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-packaging-reference.md`
**Context:** The reference file for 'packaging' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 862. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-pandocfilters-reference.md`
**Context:** The reference file for 'pandocfilters' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 863. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-pandocfilters-reference.md`
**Context:** The reference file for 'pandocfilters' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 864. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-pandocfilters-reference.md`
**Context:** The reference file for 'pandocfilters' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 865. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-panel-reference.md`
**Context:** The reference file for 'panel' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 866. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-panel-reference.md`
**Context:** The reference file for 'panel' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 867. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-panel-reference.md`
**Context:** The reference file for 'panel' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 868. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-param-reference.md`
**Context:** The reference file for 'param' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 869. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-param-reference.md`
**Context:** The reference file for 'param' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 870. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-param-reference.md`
**Context:** The reference file for 'param' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 871. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-parsel-reference.md`
**Context:** The reference file for 'parsel' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 872. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-parsel-reference.md`
**Context:** The reference file for 'parsel' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 873. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-parsel-reference.md`
**Context:** The reference file for 'parsel' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 874. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-parso-reference.md`
**Context:** The reference file for 'parso' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 875. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-parso-reference.md`
**Context:** The reference file for 'parso' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 876. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-parso-reference.md`
**Context:** The reference file for 'parso' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 877. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-partd-reference.md`
**Context:** The reference file for 'partd' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 878. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-partd-reference.md`
**Context:** The reference file for 'partd' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 879. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-partd-reference.md`
**Context:** The reference file for 'partd' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 880. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-pathlib-reference.md`
**Context:** The reference file for 'pathlib' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 881. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-pathlib-reference.md`
**Context:** The reference file for 'pathlib' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 882. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-pathlib-reference.md`
**Context:** The reference file for 'pathlib' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 883. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-pathspec-reference.md`
**Context:** The reference file for 'pathspec' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 884. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-pathspec-reference.md`
**Context:** The reference file for 'pathspec' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 885. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-pathspec-reference.md`
**Context:** The reference file for 'pathspec' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 886. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-patsy-reference.md`
**Context:** The reference file for 'patsy' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 887. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-patsy-reference.md`
**Context:** The reference file for 'patsy' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 888. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-patsy-reference.md`
**Context:** The reference file for 'patsy' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 889. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-pep8-reference.md`
**Context:** The reference file for 'pep8' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 890. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-pep8-reference.md`
**Context:** The reference file for 'pep8' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 891. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-pep8-reference.md`
**Context:** The reference file for 'pep8' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 892. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-pexpect-reference.md`
**Context:** The reference file for 'pexpect' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 893. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-pexpect-reference.md`
**Context:** The reference file for 'pexpect' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 894. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-pexpect-reference.md`
**Context:** The reference file for 'pexpect' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 895. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-pickleshare-reference.md`
**Context:** The reference file for 'pickleshare' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 896. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-pickleshare-reference.md`
**Context:** The reference file for 'pickleshare' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 897. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-pickleshare-reference.md`
**Context:** The reference file for 'pickleshare' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 898. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-pillow-reference.md`
**Context:** The reference file for 'pillow' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 899. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-pillow-reference.md`
**Context:** The reference file for 'pillow' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 900. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-pillow-reference.md`
**Context:** The reference file for 'pillow' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 901. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-pip-reference.md`
**Context:** The reference file for 'pip' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 902. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-pip-reference.md`
**Context:** The reference file for 'pip' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 903. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-pip-reference.md`
**Context:** The reference file for 'pip' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 904. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-pkce-reference.md`
**Context:** The reference file for 'pkce' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 905. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-pkce-reference.md`
**Context:** The reference file for 'pkce' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 906. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-pkce-reference.md`
**Context:** The reference file for 'pkce' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 907. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-pkginfo-reference.md`
**Context:** The reference file for 'pkginfo' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 908. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-pkginfo-reference.md`
**Context:** The reference file for 'pkginfo' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 909. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-pkginfo-reference.md`
**Context:** The reference file for 'pkginfo' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 910. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-platformdirs-reference.md`
**Context:** The reference file for 'platformdirs' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 911. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-platformdirs-reference.md`
**Context:** The reference file for 'platformdirs' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 912. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-platformdirs-reference.md`
**Context:** The reference file for 'platformdirs' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 913. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-pluggy-reference.md`
**Context:** The reference file for 'pluggy' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 914. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-pluggy-reference.md`
**Context:** The reference file for 'pluggy' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 915. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-pluggy-reference.md`
**Context:** The reference file for 'pluggy' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 916. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-powerlaw-reference.md`
**Context:** The reference file for 'powerlaw' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 917. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-powerlaw-reference.md`
**Context:** The reference file for 'powerlaw' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 918. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-powerlaw-reference.md`
**Context:** The reference file for 'powerlaw' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 919. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-powershellshortcut-reference.md`
**Context:** The reference file for 'powershell_shortcut' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 920. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-powershellshortcut-reference.md`
**Context:** The reference file for 'powershell_shortcut' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 921. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-powershellshortcut-reference.md`
**Context:** The reference file for 'powershell_shortcut' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 922. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-poyo-reference.md`
**Context:** The reference file for 'poyo' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 923. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-poyo-reference.md`
**Context:** The reference file for 'poyo' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 924. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-poyo-reference.md`
**Context:** The reference file for 'poyo' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 925. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-proj-reference.md`
**Context:** The reference file for 'proj' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 926. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-proj-reference.md`
**Context:** The reference file for 'proj' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 927. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-proj-reference.md`
**Context:** The reference file for 'proj' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 928. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-prometheusclient-reference.md`
**Context:** The reference file for 'prometheus_client' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 929. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-prometheusclient-reference.md`
**Context:** The reference file for 'prometheus_client' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 930. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-prometheusclient-reference.md`
**Context:** The reference file for 'prometheus_client' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 931. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-prompt-toolkit-reference.md`
**Context:** The reference file for 'prompt-toolkit' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 932. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-prompt-toolkit-reference.md`
**Context:** The reference file for 'prompt-toolkit' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 933. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-prompt-toolkit-reference.md`
**Context:** The reference file for 'prompt-toolkit' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 934. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-prompttoolkit-reference.md`
**Context:** The reference file for 'prompt_toolkit' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 935. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-prompttoolkit-reference.md`
**Context:** The reference file for 'prompt_toolkit' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 936. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-prompttoolkit-reference.md`
**Context:** The reference file for 'prompt_toolkit' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 937. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-property-cached-reference.md`
**Context:** The reference file for 'property-cached' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 938. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-property-cached-reference.md`
**Context:** The reference file for 'property-cached' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 939. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-property-cached-reference.md`
**Context:** The reference file for 'property-cached' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 940. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-protego-reference.md`
**Context:** The reference file for 'protego' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 941. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-protego-reference.md`
**Context:** The reference file for 'protego' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 942. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-protego-reference.md`
**Context:** The reference file for 'protego' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 943. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-psutil-reference.md`
**Context:** The reference file for 'psutil' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 944. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-psutil-reference.md`
**Context:** The reference file for 'psutil' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 945. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-psutil-reference.md`
**Context:** The reference file for 'psutil' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 946. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-psycopg2-reference.md`
**Context:** The reference file for 'psycopg2' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 947. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-psycopg2-reference.md`
**Context:** The reference file for 'psycopg2' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 948. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-psycopg2-reference.md`
**Context:** The reference file for 'psycopg2' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 949. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-ptyprocess-reference.md`
**Context:** The reference file for 'ptyprocess' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 950. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-ptyprocess-reference.md`
**Context:** The reference file for 'ptyprocess' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 951. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-ptyprocess-reference.md`
**Context:** The reference file for 'ptyprocess' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 952. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-pulp-reference.md`
**Context:** The reference file for 'pulp' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 953. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-pulp-reference.md`
**Context:** The reference file for 'pulp' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 954. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-pulp-reference.md`
**Context:** The reference file for 'pulp' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 955. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-py-reference.md`
**Context:** The reference file for 'py' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 956. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-py-reference.md`
**Context:** The reference file for 'py' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 957. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-py-reference.md`
**Context:** The reference file for 'py' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 958. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-py-lief-reference.md`
**Context:** The reference file for 'py-lief' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 959. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-py-lief-reference.md`
**Context:** The reference file for 'py-lief' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 960. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-py-lief-reference.md`
**Context:** The reference file for 'py-lief' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 961. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-pyarrow-reference.md`
**Context:** The reference file for 'pyarrow' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 962. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-pyarrow-reference.md`
**Context:** The reference file for 'pyarrow' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 963. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-pyarrow-reference.md`
**Context:** The reference file for 'pyarrow' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 964. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-pyasn1-reference.md`
**Context:** The reference file for 'pyasn1' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 965. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-pyasn1-reference.md`
**Context:** The reference file for 'pyasn1' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 966. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-pyasn1-reference.md`
**Context:** The reference file for 'pyasn1' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 967. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-pyasn1-modules-reference.md`
**Context:** The reference file for 'pyasn1-modules' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 968. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-pyasn1-modules-reference.md`
**Context:** The reference file for 'pyasn1-modules' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 969. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-pyasn1-modules-reference.md`
**Context:** The reference file for 'pyasn1-modules' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 970. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-pycodestyle-reference.md`
**Context:** The reference file for 'pycodestyle' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 971. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-pycodestyle-reference.md`
**Context:** The reference file for 'pycodestyle' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 972. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-pycodestyle-reference.md`
**Context:** The reference file for 'pycodestyle' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 973. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-pycosat-reference.md`
**Context:** The reference file for 'pycosat' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 974. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-pycosat-reference.md`
**Context:** The reference file for 'pycosat' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 975. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-pycosat-reference.md`
**Context:** The reference file for 'pycosat' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 976. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-pycparser-reference.md`
**Context:** The reference file for 'pycparser' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 977. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-pycparser-reference.md`
**Context:** The reference file for 'pycparser' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 978. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-pycparser-reference.md`
**Context:** The reference file for 'pycparser' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 979. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-pyct-reference.md`
**Context:** The reference file for 'pyct' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 980. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-pyct-reference.md`
**Context:** The reference file for 'pyct' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 981. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-pyct-reference.md`
**Context:** The reference file for 'pyct' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 982. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-pycurl-reference.md`
**Context:** The reference file for 'pycurl' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 983. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-pycurl-reference.md`
**Context:** The reference file for 'pycurl' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 984. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-pycurl-reference.md`
**Context:** The reference file for 'pycurl' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 985. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-pydantic-reference.md`
**Context:** The reference file for 'pydantic' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 986. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-pydantic-reference.md`
**Context:** The reference file for 'pydantic' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 987. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-pydantic-reference.md`
**Context:** The reference file for 'pydantic' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 988. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-pydispatcher-reference.md`
**Context:** The reference file for 'pydispatcher' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 989. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-pydispatcher-reference.md`
**Context:** The reference file for 'pydispatcher' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 990. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-pydispatcher-reference.md`
**Context:** The reference file for 'pydispatcher' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 991. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-pydocstyle-reference.md`
**Context:** The reference file for 'pydocstyle' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 992. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-pydocstyle-reference.md`
**Context:** The reference file for 'pydocstyle' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 993. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-pydocstyle-reference.md`
**Context:** The reference file for 'pydocstyle' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 994. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-pyerfa-reference.md`
**Context:** The reference file for 'pyerfa' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 995. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-pyerfa-reference.md`
**Context:** The reference file for 'pyerfa' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 996. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-pyerfa-reference.md`
**Context:** The reference file for 'pyerfa' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 997. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-pyflakes-reference.md`
**Context:** The reference file for 'pyflakes' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 998. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-pyflakes-reference.md`
**Context:** The reference file for 'pyflakes' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 999. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-pyflakes-reference.md`
**Context:** The reference file for 'pyflakes' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 1000. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-pyflux-reference.md`
**Context:** The reference file for 'pyflux' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 1001. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-pyflux-reference.md`
**Context:** The reference file for 'pyflux' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 1002. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-pyflux-reference.md`
**Context:** The reference file for 'pyflux' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 1003. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-pygments-reference.md`
**Context:** The reference file for 'pygments' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 1004. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-pygments-reference.md`
**Context:** The reference file for 'pygments' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 1005. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-pygments-reference.md`
**Context:** The reference file for 'pygments' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 1006. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-pyhamcrest-reference.md`
**Context:** The reference file for 'pyhamcrest' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 1007. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-pyhamcrest-reference.md`
**Context:** The reference file for 'pyhamcrest' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 1008. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-pyhamcrest-reference.md`
**Context:** The reference file for 'pyhamcrest' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 1009. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-pyjwt-reference.md`
**Context:** The reference file for 'pyjwt' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 1010. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-pyjwt-reference.md`
**Context:** The reference file for 'pyjwt' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 1011. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-pyjwt-reference.md`
**Context:** The reference file for 'pyjwt' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 1012. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-pylint-reference.md`
**Context:** The reference file for 'pylint' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 1013. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-pylint-reference.md`
**Context:** The reference file for 'pylint' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 1014. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-pylint-reference.md`
**Context:** The reference file for 'pylint' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 1015. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-pyls-spyder-reference.md`
**Context:** The reference file for 'pyls-spyder' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 1016. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-pyls-spyder-reference.md`
**Context:** The reference file for 'pyls-spyder' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 1017. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-pyls-spyder-reference.md`
**Context:** The reference file for 'pyls-spyder' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 1018. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-pynacl-reference.md`
**Context:** The reference file for 'pynacl' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 1019. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-pynacl-reference.md`
**Context:** The reference file for 'pynacl' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 1020. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-pynacl-reference.md`
**Context:** The reference file for 'pynacl' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 1021. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-pyodbc-reference.md`
**Context:** The reference file for 'pyodbc' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 1022. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-pyodbc-reference.md`
**Context:** The reference file for 'pyodbc' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 1023. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-pyodbc-reference.md`
**Context:** The reference file for 'pyodbc' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 1024. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-pyogrio-reference.md`
**Context:** The reference file for 'pyogrio' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 1025. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-pyogrio-reference.md`
**Context:** The reference file for 'pyogrio' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 1026. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-pyogrio-reference.md`
**Context:** The reference file for 'pyogrio' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 1027. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-pyopenssl-reference.md`
**Context:** The reference file for 'pyopenssl' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 1028. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-pyopenssl-reference.md`
**Context:** The reference file for 'pyopenssl' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 1029. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-pyopenssl-reference.md`
**Context:** The reference file for 'pyopenssl' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 1030. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-pyparsing-reference.md`
**Context:** The reference file for 'pyparsing' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 1031. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-pyparsing-reference.md`
**Context:** The reference file for 'pyparsing' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 1032. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-pyparsing-reference.md`
**Context:** The reference file for 'pyparsing' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 1033. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-pyprind-reference.md`
**Context:** The reference file for 'pyprind' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 1034. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-pyprind-reference.md`
**Context:** The reference file for 'pyprind' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 1035. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-pyprind-reference.md`
**Context:** The reference file for 'pyprind' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 1036. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-pyproj-reference.md`
**Context:** The reference file for 'pyproj' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 1037. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-pyproj-reference.md`
**Context:** The reference file for 'pyproj' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 1038. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-pyproj-reference.md`
**Context:** The reference file for 'pyproj' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 1039. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-pyqt-reference.md`
**Context:** The reference file for 'pyqt' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 1040. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-pyqt-reference.md`
**Context:** The reference file for 'pyqt' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 1041. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-pyqt-reference.md`
**Context:** The reference file for 'pyqt' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 1042. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-pyrsistent-reference.md`
**Context:** The reference file for 'pyrsistent' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 1043. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-pyrsistent-reference.md`
**Context:** The reference file for 'pyrsistent' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 1044. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-pyrsistent-reference.md`
**Context:** The reference file for 'pyrsistent' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 1045. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-pysocks-reference.md`
**Context:** The reference file for 'pysocks' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 1046. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-pysocks-reference.md`
**Context:** The reference file for 'pysocks' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 1047. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-pysocks-reference.md`
**Context:** The reference file for 'pysocks' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 1048. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-pytables-reference.md`
**Context:** The reference file for 'pytables' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 1049. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-pytables-reference.md`
**Context:** The reference file for 'pytables' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 1050. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-pytables-reference.md`
**Context:** The reference file for 'pytables' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 1051. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-pytest-reference.md`
**Context:** The reference file for 'pytest' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 1052. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-pytest-reference.md`
**Context:** The reference file for 'pytest' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 1053. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-pytest-reference.md`
**Context:** The reference file for 'pytest' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 1054. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-python-reference.md`
**Context:** The reference file for 'python' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 1055. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-python-reference.md`
**Context:** The reference file for 'python' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 1056. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-python-reference.md`
**Context:** The reference file for 'python' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 1057. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-python-dateutil-reference.md`
**Context:** The reference file for 'python-dateutil' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 1058. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-python-dateutil-reference.md`
**Context:** The reference file for 'python-dateutil' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 1059. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-python-dateutil-reference.md`
**Context:** The reference file for 'python-dateutil' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 1060. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-python-dotenv-reference.md`
**Context:** The reference file for 'python-dotenv' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 1061. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-python-dotenv-reference.md`
**Context:** The reference file for 'python-dotenv' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 1062. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-python-dotenv-reference.md`
**Context:** The reference file for 'python-dotenv' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 1063. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-python-fastjsonschema-reference.md`
**Context:** The reference file for 'python-fastjsonschema' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 1064. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-python-fastjsonschema-reference.md`
**Context:** The reference file for 'python-fastjsonschema' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 1065. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-python-fastjsonschema-reference.md`
**Context:** The reference file for 'python-fastjsonschema' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 1066. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-python-libarchive-c-reference.md`
**Context:** The reference file for 'python-libarchive-c' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 1067. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-python-libarchive-c-reference.md`
**Context:** The reference file for 'python-libarchive-c' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 1068. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-python-libarchive-c-reference.md`
**Context:** The reference file for 'python-libarchive-c' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 1069. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-python-lsp-black-reference.md`
**Context:** The reference file for 'python-lsp-black' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 1070. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-python-lsp-black-reference.md`
**Context:** The reference file for 'python-lsp-black' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 1071. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-python-lsp-black-reference.md`
**Context:** The reference file for 'python-lsp-black' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 1072. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-python-lsp-jsonrpc-reference.md`
**Context:** The reference file for 'python-lsp-jsonrpc' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 1073. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-python-lsp-jsonrpc-reference.md`
**Context:** The reference file for 'python-lsp-jsonrpc' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 1074. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-python-lsp-jsonrpc-reference.md`
**Context:** The reference file for 'python-lsp-jsonrpc' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 1075. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-python-lsp-server-reference.md`
**Context:** The reference file for 'python-lsp-server' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 1076. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-python-lsp-server-reference.md`
**Context:** The reference file for 'python-lsp-server' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 1077. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-python-lsp-server-reference.md`
**Context:** The reference file for 'python-lsp-server' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 1078. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-python-slugify-reference.md`
**Context:** The reference file for 'python-slugify' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 1079. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-python-slugify-reference.md`
**Context:** The reference file for 'python-slugify' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 1080. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-python-slugify-reference.md`
**Context:** The reference file for 'python-slugify' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 1081. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-python-snappy-reference.md`
**Context:** The reference file for 'python-snappy' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 1082. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-python-snappy-reference.md`
**Context:** The reference file for 'python-snappy' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 1083. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-python-snappy-reference.md`
**Context:** The reference file for 'python-snappy' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 1084. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-pythonabi-reference.md`
**Context:** The reference file for 'python_abi' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 1085. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-pythonabi-reference.md`
**Context:** The reference file for 'python_abi' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 1086. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-pythonabi-reference.md`
**Context:** The reference file for 'python_abi' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 1087. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-pytz-reference.md`
**Context:** The reference file for 'pytz' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 1088. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-pytz-reference.md`
**Context:** The reference file for 'pytz' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 1089. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-pytz-reference.md`
**Context:** The reference file for 'pytz' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 1090. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-pyusda-reference.md`
**Context:** The reference file for 'pyusda' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 1091. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-pyusda-reference.md`
**Context:** The reference file for 'pyusda' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 1092. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-pyusda-reference.md`
**Context:** The reference file for 'pyusda' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 1093. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-pyvizcomms-reference.md`
**Context:** The reference file for 'pyviz_comms' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 1094. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-pyvizcomms-reference.md`
**Context:** The reference file for 'pyviz_comms' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 1095. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-pyvizcomms-reference.md`
**Context:** The reference file for 'pyviz_comms' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 1096. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-pywavelets-reference.md`
**Context:** The reference file for 'pywavelets' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 1097. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-pywavelets-reference.md`
**Context:** The reference file for 'pywavelets' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 1098. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-pywavelets-reference.md`
**Context:** The reference file for 'pywavelets' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 1099. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-pywin32-reference.md`
**Context:** The reference file for 'pywin32' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 1100. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-pywin32-reference.md`
**Context:** The reference file for 'pywin32' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 1101. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-pywin32-reference.md`
**Context:** The reference file for 'pywin32' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 1102. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-pywin32-ctypes-reference.md`
**Context:** The reference file for 'pywin32-ctypes' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 1103. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-pywin32-ctypes-reference.md`
**Context:** The reference file for 'pywin32-ctypes' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 1104. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-pywin32-ctypes-reference.md`
**Context:** The reference file for 'pywin32-ctypes' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 1105. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-pywinpty-reference.md`
**Context:** The reference file for 'pywinpty' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 1106. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-pywinpty-reference.md`
**Context:** The reference file for 'pywinpty' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 1107. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-pywinpty-reference.md`
**Context:** The reference file for 'pywinpty' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 1108. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-pyyaml-reference.md`
**Context:** The reference file for 'pyyaml' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 1109. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-pyyaml-reference.md`
**Context:** The reference file for 'pyyaml' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 1110. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-pyyaml-reference.md`
**Context:** The reference file for 'pyyaml' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 1111. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-pyzmq-reference.md`
**Context:** The reference file for 'pyzmq' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 1112. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-pyzmq-reference.md`
**Context:** The reference file for 'pyzmq' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 1113. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-pyzmq-reference.md`
**Context:** The reference file for 'pyzmq' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 1114. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-qdarkstyle-reference.md`
**Context:** The reference file for 'qdarkstyle' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 1115. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-qdarkstyle-reference.md`
**Context:** The reference file for 'qdarkstyle' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 1116. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-qdarkstyle-reference.md`
**Context:** The reference file for 'qdarkstyle' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 1117. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-qdldl-reference.md`
**Context:** The reference file for 'qdldl' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 1118. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-qdldl-reference.md`
**Context:** The reference file for 'qdldl' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 1119. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-qdldl-reference.md`
**Context:** The reference file for 'qdldl' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 1120. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-qstylizer-reference.md`
**Context:** The reference file for 'qstylizer' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 1121. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-qstylizer-reference.md`
**Context:** The reference file for 'qstylizer' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 1122. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-qstylizer-reference.md`
**Context:** The reference file for 'qstylizer' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 1123. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-qt-reference.md`
**Context:** The reference file for 'qt' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 1124. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-qt-reference.md`
**Context:** The reference file for 'qt' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 1125. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-qt-reference.md`
**Context:** The reference file for 'qt' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 1126. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-qtawesome-reference.md`
**Context:** The reference file for 'qtawesome' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 1127. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-qtawesome-reference.md`
**Context:** The reference file for 'qtawesome' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 1128. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-qtawesome-reference.md`
**Context:** The reference file for 'qtawesome' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 1129. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-qtconsole-reference.md`
**Context:** The reference file for 'qtconsole' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 1130. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-qtconsole-reference.md`
**Context:** The reference file for 'qtconsole' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 1131. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-qtconsole-reference.md`
**Context:** The reference file for 'qtconsole' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 1132. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-qtpy-reference.md`
**Context:** The reference file for 'qtpy' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 1133. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-qtpy-reference.md`
**Context:** The reference file for 'qtpy' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 1134. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-qtpy-reference.md`
**Context:** The reference file for 'qtpy' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 1135. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-queuelib-reference.md`
**Context:** The reference file for 'queuelib' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 1136. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-queuelib-reference.md`
**Context:** The reference file for 'queuelib' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 1137. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-queuelib-reference.md`
**Context:** The reference file for 'queuelib' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 1138. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-random-dict-reference.md`
**Context:** The reference file for 'random-dict' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 1139. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-random-dict-reference.md`
**Context:** The reference file for 'random-dict' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 1140. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-random-dict-reference.md`
**Context:** The reference file for 'random-dict' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 1141. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-randomdict-reference.md`
**Context:** The reference file for 'randomdict' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 1142. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-randomdict-reference.md`
**Context:** The reference file for 'randomdict' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 1143. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-randomdict-reference.md`
**Context:** The reference file for 'randomdict' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 1144. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-re2-reference.md`
**Context:** The reference file for 're2' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 1145. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-re2-reference.md`
**Context:** The reference file for 're2' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 1146. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-re2-reference.md`
**Context:** The reference file for 're2' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 1147. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-regex-reference.md`
**Context:** The reference file for 'regex' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 1148. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-regex-reference.md`
**Context:** The reference file for 'regex' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 1149. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-regex-reference.md`
**Context:** The reference file for 'regex' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 1150. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-requests-reference.md`
**Context:** The reference file for 'requests' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 1151. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-requests-reference.md`
**Context:** The reference file for 'requests' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 1152. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-requests-reference.md`
**Context:** The reference file for 'requests' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 1153. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-requests-file-reference.md`
**Context:** The reference file for 'requests-file' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 1154. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-requests-file-reference.md`
**Context:** The reference file for 'requests-file' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 1155. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-requests-file-reference.md`
**Context:** The reference file for 'requests-file' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 1156. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-retrying-reference.md`
**Context:** The reference file for 'retrying' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 1157. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-retrying-reference.md`
**Context:** The reference file for 'retrying' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 1158. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-retrying-reference.md`
**Context:** The reference file for 'retrying' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 1159. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-rope-reference.md`
**Context:** The reference file for 'rope' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 1160. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-rope-reference.md`
**Context:** The reference file for 'rope' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 1161. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-rope-reference.md`
**Context:** The reference file for 'rope' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 1162. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-rtree-reference.md`
**Context:** The reference file for 'rtree' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 1163. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-rtree-reference.md`
**Context:** The reference file for 'rtree' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 1164. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-rtree-reference.md`
**Context:** The reference file for 'rtree' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 1165. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-ruamelyaml-reference.md`
**Context:** The reference file for 'ruamel.yaml' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 1166. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-ruamelyaml-reference.md`
**Context:** The reference file for 'ruamel.yaml' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 1167. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-ruamelyaml-reference.md`
**Context:** The reference file for 'ruamel.yaml' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 1168. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-ruamelyamlclib-reference.md`
**Context:** The reference file for 'ruamel.yaml.clib' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 1169. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-ruamelyamlclib-reference.md`
**Context:** The reference file for 'ruamel.yaml.clib' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 1170. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-ruamelyamlclib-reference.md`
**Context:** The reference file for 'ruamel.yaml.clib' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 1171. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-s3transfer-reference.md`
**Context:** The reference file for 's3transfer' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 1172. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-s3transfer-reference.md`
**Context:** The reference file for 's3transfer' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 1173. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-s3transfer-reference.md`
**Context:** The reference file for 's3transfer' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 1174. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-scikit-image-reference.md`
**Context:** The reference file for 'scikit-image' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 1175. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-scikit-image-reference.md`
**Context:** The reference file for 'scikit-image' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 1176. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-scikit-image-reference.md`
**Context:** The reference file for 'scikit-image' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 1177. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-scikit-learn-reference.md`
**Context:** The reference file for 'scikit-learn' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 1178. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-scikit-learn-reference.md`
**Context:** The reference file for 'scikit-learn' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 1179. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-scikit-learn-reference.md`
**Context:** The reference file for 'scikit-learn' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 1180. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-scikit-learn-intelex-reference.md`
**Context:** The reference file for 'scikit-learn-intelex' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 1181. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-scikit-learn-intelex-reference.md`
**Context:** The reference file for 'scikit-learn-intelex' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 1182. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-scikit-learn-intelex-reference.md`
**Context:** The reference file for 'scikit-learn-intelex' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 1183. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-scipy-reference.md`
**Context:** The reference file for 'scipy' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 1184. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-scipy-reference.md`
**Context:** The reference file for 'scipy' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 1185. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-scipy-reference.md`
**Context:** The reference file for 'scipy' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 1186. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-scrapy-reference.md`
**Context:** The reference file for 'scrapy' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 1187. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-scrapy-reference.md`
**Context:** The reference file for 'scrapy' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 1188. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-scrapy-reference.md`
**Context:** The reference file for 'scrapy' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 1189. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-scs-reference.md`
**Context:** The reference file for 'scs' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 1190. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-scs-reference.md`
**Context:** The reference file for 'scs' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 1191. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-scs-reference.md`
**Context:** The reference file for 'scs' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 1192. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-seaborn-reference.md`
**Context:** The reference file for 'seaborn' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 1193. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-seaborn-reference.md`
**Context:** The reference file for 'seaborn' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 1194. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-seaborn-reference.md`
**Context:** The reference file for 'seaborn' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 1195. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-semver-reference.md`
**Context:** The reference file for 'semver' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 1196. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-semver-reference.md`
**Context:** The reference file for 'semver' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 1197. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-semver-reference.md`
**Context:** The reference file for 'semver' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 1198. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-send2trash-reference.md`
**Context:** The reference file for 'send2trash' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 1199. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-send2trash-reference.md`
**Context:** The reference file for 'send2trash' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 1200. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-send2trash-reference.md`
**Context:** The reference file for 'send2trash' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 1201. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-serviceidentity-reference.md`
**Context:** The reference file for 'service_identity' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 1202. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-serviceidentity-reference.md`
**Context:** The reference file for 'service_identity' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 1203. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-serviceidentity-reference.md`
**Context:** The reference file for 'service_identity' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 1204. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-setuptools-reference.md`
**Context:** The reference file for 'setuptools' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 1205. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-setuptools-reference.md`
**Context:** The reference file for 'setuptools' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 1206. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-setuptools-reference.md`
**Context:** The reference file for 'setuptools' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 1207. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-shapely-reference.md`
**Context:** The reference file for 'shapely' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 1208. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-shapely-reference.md`
**Context:** The reference file for 'shapely' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 1209. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-shapely-reference.md`
**Context:** The reference file for 'shapely' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 1210. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-sip-reference.md`
**Context:** The reference file for 'sip' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 1211. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-sip-reference.md`
**Context:** The reference file for 'sip' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 1212. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-sip-reference.md`
**Context:** The reference file for 'sip' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 1213. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-six-reference.md`
**Context:** The reference file for 'six' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 1214. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-six-reference.md`
**Context:** The reference file for 'six' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 1215. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-six-reference.md`
**Context:** The reference file for 'six' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 1216. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-smartopen-reference.md`
**Context:** The reference file for 'smart_open' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 1217. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-smartopen-reference.md`
**Context:** The reference file for 'smart_open' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 1218. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-smartopen-reference.md`
**Context:** The reference file for 'smart_open' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 1219. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-snappy-reference.md`
**Context:** The reference file for 'snappy' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 1220. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-snappy-reference.md`
**Context:** The reference file for 'snappy' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 1221. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-snappy-reference.md`
**Context:** The reference file for 'snappy' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 1222. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-sniffio-reference.md`
**Context:** The reference file for 'sniffio' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 1223. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-sniffio-reference.md`
**Context:** The reference file for 'sniffio' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 1224. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-sniffio-reference.md`
**Context:** The reference file for 'sniffio' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 1225. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-snowballstemmer-reference.md`
**Context:** The reference file for 'snowballstemmer' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 1226. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-snowballstemmer-reference.md`
**Context:** The reference file for 'snowballstemmer' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 1227. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-snowballstemmer-reference.md`
**Context:** The reference file for 'snowballstemmer' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 1228. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-sortedcollections-reference.md`
**Context:** The reference file for 'sortedcollections' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 1229. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-sortedcollections-reference.md`
**Context:** The reference file for 'sortedcollections' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 1230. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-sortedcollections-reference.md`
**Context:** The reference file for 'sortedcollections' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 1231. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-sortedcontainers-reference.md`
**Context:** The reference file for 'sortedcontainers' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 1232. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-sortedcontainers-reference.md`
**Context:** The reference file for 'sortedcontainers' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 1233. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-sortedcontainers-reference.md`
**Context:** The reference file for 'sortedcontainers' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 1234. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-soupsieve-reference.md`
**Context:** The reference file for 'soupsieve' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 1235. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-soupsieve-reference.md`
**Context:** The reference file for 'soupsieve' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 1236. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-soupsieve-reference.md`
**Context:** The reference file for 'soupsieve' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 1237. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-sphinx-reference.md`
**Context:** The reference file for 'sphinx' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 1238. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-sphinx-reference.md`
**Context:** The reference file for 'sphinx' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 1239. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-sphinx-reference.md`
**Context:** The reference file for 'sphinx' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 1240. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-sphinxcontrib-applehelp-reference.md`
**Context:** The reference file for 'sphinxcontrib-applehelp' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 1241. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-sphinxcontrib-applehelp-reference.md`
**Context:** The reference file for 'sphinxcontrib-applehelp' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 1242. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-sphinxcontrib-applehelp-reference.md`
**Context:** The reference file for 'sphinxcontrib-applehelp' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 1243. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-sphinxcontrib-devhelp-reference.md`
**Context:** The reference file for 'sphinxcontrib-devhelp' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 1244. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-sphinxcontrib-devhelp-reference.md`
**Context:** The reference file for 'sphinxcontrib-devhelp' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 1245. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-sphinxcontrib-devhelp-reference.md`
**Context:** The reference file for 'sphinxcontrib-devhelp' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 1246. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-sphinxcontrib-htmlhelp-reference.md`
**Context:** The reference file for 'sphinxcontrib-htmlhelp' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 1247. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-sphinxcontrib-htmlhelp-reference.md`
**Context:** The reference file for 'sphinxcontrib-htmlhelp' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 1248. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-sphinxcontrib-htmlhelp-reference.md`
**Context:** The reference file for 'sphinxcontrib-htmlhelp' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 1249. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-sphinxcontrib-jsmath-reference.md`
**Context:** The reference file for 'sphinxcontrib-jsmath' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 1250. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-sphinxcontrib-jsmath-reference.md`
**Context:** The reference file for 'sphinxcontrib-jsmath' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 1251. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-sphinxcontrib-jsmath-reference.md`
**Context:** The reference file for 'sphinxcontrib-jsmath' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 1252. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-sphinxcontrib-qthelp-reference.md`
**Context:** The reference file for 'sphinxcontrib-qthelp' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 1253. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-sphinxcontrib-qthelp-reference.md`
**Context:** The reference file for 'sphinxcontrib-qthelp' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 1254. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-sphinxcontrib-qthelp-reference.md`
**Context:** The reference file for 'sphinxcontrib-qthelp' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 1255. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-sphinxcontrib-serializinghtml-reference.md`
**Context:** The reference file for 'sphinxcontrib-serializinghtml' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 1256. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-sphinxcontrib-serializinghtml-reference.md`
**Context:** The reference file for 'sphinxcontrib-serializinghtml' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 1257. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-sphinxcontrib-serializinghtml-reference.md`
**Context:** The reference file for 'sphinxcontrib-serializinghtml' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 1258. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-spyder-reference.md`
**Context:** The reference file for 'spyder' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 1259. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-spyder-reference.md`
**Context:** The reference file for 'spyder' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 1260. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-spyder-reference.md`
**Context:** The reference file for 'spyder' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 1261. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-spyder-kernels-reference.md`
**Context:** The reference file for 'spyder-kernels' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 1262. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-spyder-kernels-reference.md`
**Context:** The reference file for 'spyder-kernels' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 1263. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-spyder-kernels-reference.md`
**Context:** The reference file for 'spyder-kernels' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 1264. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-sqlalchemy-reference.md`
**Context:** The reference file for 'sqlalchemy' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 1265. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-sqlalchemy-reference.md`
**Context:** The reference file for 'sqlalchemy' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 1266. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-sqlalchemy-reference.md`
**Context:** The reference file for 'sqlalchemy' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 1267. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-statsmodels-reference.md`
**Context:** The reference file for 'statsmodels' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 1268. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-statsmodels-reference.md`
**Context:** The reference file for 'statsmodels' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 1269. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-statsmodels-reference.md`
**Context:** The reference file for 'statsmodels' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 1270. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-sympy-reference.md`
**Context:** The reference file for 'sympy' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 1271. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-sympy-reference.md`
**Context:** The reference file for 'sympy' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 1272. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-sympy-reference.md`
**Context:** The reference file for 'sympy' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 1273. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-tabulate-reference.md`
**Context:** The reference file for 'tabulate' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 1274. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-tabulate-reference.md`
**Context:** The reference file for 'tabulate' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 1275. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-tabulate-reference.md`
**Context:** The reference file for 'tabulate' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 1276. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-tbb-reference.md`
**Context:** The reference file for 'tbb' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 1277. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-tbb-reference.md`
**Context:** The reference file for 'tbb' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 1278. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-tbb-reference.md`
**Context:** The reference file for 'tbb' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 1279. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-tbb4py-reference.md`
**Context:** The reference file for 'tbb4py' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 1280. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-tbb4py-reference.md`
**Context:** The reference file for 'tbb4py' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 1281. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-tbb4py-reference.md`
**Context:** The reference file for 'tbb4py' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 1282. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-tblib-reference.md`
**Context:** The reference file for 'tblib' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 1283. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-tblib-reference.md`
**Context:** The reference file for 'tblib' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 1284. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-tblib-reference.md`
**Context:** The reference file for 'tblib' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 1285. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-tenacity-reference.md`
**Context:** The reference file for 'tenacity' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 1286. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-tenacity-reference.md`
**Context:** The reference file for 'tenacity' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 1287. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-tenacity-reference.md`
**Context:** The reference file for 'tenacity' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 1288. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-terminado-reference.md`
**Context:** The reference file for 'terminado' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 1289. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-terminado-reference.md`
**Context:** The reference file for 'terminado' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 1290. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-terminado-reference.md`
**Context:** The reference file for 'terminado' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 1291. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-testpath-reference.md`
**Context:** The reference file for 'testpath' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 1292. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-testpath-reference.md`
**Context:** The reference file for 'testpath' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 1293. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-testpath-reference.md`
**Context:** The reference file for 'testpath' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 1294. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-text-unidecode-reference.md`
**Context:** The reference file for 'text-unidecode' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 1295. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-text-unidecode-reference.md`
**Context:** The reference file for 'text-unidecode' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 1296. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-text-unidecode-reference.md`
**Context:** The reference file for 'text-unidecode' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 1297. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-textdistance-reference.md`
**Context:** The reference file for 'textdistance' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 1298. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-textdistance-reference.md`
**Context:** The reference file for 'textdistance' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 1299. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-textdistance-reference.md`
**Context:** The reference file for 'textdistance' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 1300. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-threadpoolctl-reference.md`
**Context:** The reference file for 'threadpoolctl' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 1301. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-threadpoolctl-reference.md`
**Context:** The reference file for 'threadpoolctl' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 1302. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-threadpoolctl-reference.md`
**Context:** The reference file for 'threadpoolctl' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 1303. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-three-merge-reference.md`
**Context:** The reference file for 'three-merge' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 1304. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-three-merge-reference.md`
**Context:** The reference file for 'three-merge' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 1305. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-three-merge-reference.md`
**Context:** The reference file for 'three-merge' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 1306. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-tifffile-reference.md`
**Context:** The reference file for 'tifffile' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 1307. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-tifffile-reference.md`
**Context:** The reference file for 'tifffile' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 1308. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-tifffile-reference.md`
**Context:** The reference file for 'tifffile' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 1309. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-tiledb-reference.md`
**Context:** The reference file for 'tiledb' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 1310. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-tiledb-reference.md`
**Context:** The reference file for 'tiledb' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 1311. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-tiledb-reference.md`
**Context:** The reference file for 'tiledb' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 1312. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-tinycss-reference.md`
**Context:** The reference file for 'tinycss' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 1313. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-tinycss-reference.md`
**Context:** The reference file for 'tinycss' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 1314. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-tinycss-reference.md`
**Context:** The reference file for 'tinycss' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 1315. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-tk-reference.md`
**Context:** The reference file for 'tk' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 1316. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-tk-reference.md`
**Context:** The reference file for 'tk' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 1317. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-tk-reference.md`
**Context:** The reference file for 'tk' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 1318. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-tldextract-reference.md`
**Context:** The reference file for 'tldextract' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 1319. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-tldextract-reference.md`
**Context:** The reference file for 'tldextract' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 1320. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-tldextract-reference.md`
**Context:** The reference file for 'tldextract' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 1321. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-toml-reference.md`
**Context:** The reference file for 'toml' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 1322. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-toml-reference.md`
**Context:** The reference file for 'toml' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 1323. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-toml-reference.md`
**Context:** The reference file for 'toml' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 1324. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-tomli-reference.md`
**Context:** The reference file for 'tomli' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 1325. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-tomli-reference.md`
**Context:** The reference file for 'tomli' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 1326. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-tomli-reference.md`
**Context:** The reference file for 'tomli' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 1327. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-tomlkit-reference.md`
**Context:** The reference file for 'tomlkit' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 1328. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-tomlkit-reference.md`
**Context:** The reference file for 'tomlkit' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 1329. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-tomlkit-reference.md`
**Context:** The reference file for 'tomlkit' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 1330. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-toolz-reference.md`
**Context:** The reference file for 'toolz' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 1331. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-toolz-reference.md`
**Context:** The reference file for 'toolz' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 1332. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-toolz-reference.md`
**Context:** The reference file for 'toolz' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 1333. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-tornado-reference.md`
**Context:** The reference file for 'tornado' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 1334. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-tornado-reference.md`
**Context:** The reference file for 'tornado' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 1335. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-tornado-reference.md`
**Context:** The reference file for 'tornado' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 1336. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-tqdm-reference.md`
**Context:** The reference file for 'tqdm' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 1337. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-tqdm-reference.md`
**Context:** The reference file for 'tqdm' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 1338. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-tqdm-reference.md`
**Context:** The reference file for 'tqdm' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 1339. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-traitlets-reference.md`
**Context:** The reference file for 'traitlets' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 1340. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-traitlets-reference.md`
**Context:** The reference file for 'traitlets' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 1341. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-traitlets-reference.md`
**Context:** The reference file for 'traitlets' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 1342. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-twisted-reference.md`
**Context:** The reference file for 'twisted' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 1343. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-twisted-reference.md`
**Context:** The reference file for 'twisted' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 1344. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-twisted-reference.md`
**Context:** The reference file for 'twisted' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 1345. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-twisted-iocpsupport-reference.md`
**Context:** The reference file for 'twisted-iocpsupport' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 1346. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-twisted-iocpsupport-reference.md`
**Context:** The reference file for 'twisted-iocpsupport' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 1347. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-twisted-iocpsupport-reference.md`
**Context:** The reference file for 'twisted-iocpsupport' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 1348. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-typing-extensions-reference.md`
**Context:** The reference file for 'typing-extensions' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 1349. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-typing-extensions-reference.md`
**Context:** The reference file for 'typing-extensions' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 1350. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-typing-extensions-reference.md`
**Context:** The reference file for 'typing-extensions' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 1351. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-typingextensions-reference.md`
**Context:** The reference file for 'typing_extensions' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 1352. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-typingextensions-reference.md`
**Context:** The reference file for 'typing_extensions' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 1353. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-typingextensions-reference.md`
**Context:** The reference file for 'typing_extensions' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 1354. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-tzdata-reference.md`
**Context:** The reference file for 'tzdata' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 1355. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-tzdata-reference.md`
**Context:** The reference file for 'tzdata' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 1356. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-tzdata-reference.md`
**Context:** The reference file for 'tzdata' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 1357. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-ujson-reference.md`
**Context:** The reference file for 'ujson' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 1358. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-ujson-reference.md`
**Context:** The reference file for 'ujson' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 1359. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-ujson-reference.md`
**Context:** The reference file for 'ujson' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 1360. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-unidecode-reference.md`
**Context:** The reference file for 'unidecode' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 1361. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-unidecode-reference.md`
**Context:** The reference file for 'unidecode' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 1362. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-unidecode-reference.md`
**Context:** The reference file for 'unidecode' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 1363. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-urllib3-reference.md`
**Context:** The reference file for 'urllib3' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 1364. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-urllib3-reference.md`
**Context:** The reference file for 'urllib3' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 1365. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-urllib3-reference.md`
**Context:** The reference file for 'urllib3' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 1366. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-utf8proc-reference.md`
**Context:** The reference file for 'utf8proc' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 1367. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-utf8proc-reference.md`
**Context:** The reference file for 'utf8proc' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 1368. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-utf8proc-reference.md`
**Context:** The reference file for 'utf8proc' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 1369. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-vc-reference.md`
**Context:** The reference file for 'vc' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 1370. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-vc-reference.md`
**Context:** The reference file for 'vc' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 1371. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-vc-reference.md`
**Context:** The reference file for 'vc' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 1372. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-vs2015runtime-reference.md`
**Context:** The reference file for 'vs2015_runtime' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 1373. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-vs2015runtime-reference.md`
**Context:** The reference file for 'vs2015_runtime' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 1374. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-vs2015runtime-reference.md`
**Context:** The reference file for 'vs2015_runtime' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 1375. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-w3lib-reference.md`
**Context:** The reference file for 'w3lib' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 1376. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-w3lib-reference.md`
**Context:** The reference file for 'w3lib' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 1377. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-w3lib-reference.md`
**Context:** The reference file for 'w3lib' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 1378. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-watchdog-reference.md`
**Context:** The reference file for 'watchdog' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 1379. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-watchdog-reference.md`
**Context:** The reference file for 'watchdog' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 1380. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-watchdog-reference.md`
**Context:** The reference file for 'watchdog' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 1381. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-wcwidth-reference.md`
**Context:** The reference file for 'wcwidth' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 1382. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-wcwidth-reference.md`
**Context:** The reference file for 'wcwidth' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 1383. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-wcwidth-reference.md`
**Context:** The reference file for 'wcwidth' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 1384. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-webencodings-reference.md`
**Context:** The reference file for 'webencodings' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 1385. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-webencodings-reference.md`
**Context:** The reference file for 'webencodings' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 1386. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-webencodings-reference.md`
**Context:** The reference file for 'webencodings' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 1387. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-websocket-client-reference.md`
**Context:** The reference file for 'websocket-client' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 1388. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-websocket-client-reference.md`
**Context:** The reference file for 'websocket-client' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 1389. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-websocket-client-reference.md`
**Context:** The reference file for 'websocket-client' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 1390. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-werkzeug-reference.md`
**Context:** The reference file for 'werkzeug' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 1391. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-werkzeug-reference.md`
**Context:** The reference file for 'werkzeug' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 1392. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-werkzeug-reference.md`
**Context:** The reference file for 'werkzeug' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 1393. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-wheel-reference.md`
**Context:** The reference file for 'wheel' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 1394. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-wheel-reference.md`
**Context:** The reference file for 'wheel' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 1395. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-wheel-reference.md`
**Context:** The reference file for 'wheel' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 1396. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-widgetsnbextension-reference.md`
**Context:** The reference file for 'widgetsnbextension' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 1397. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-widgetsnbextension-reference.md`
**Context:** The reference file for 'widgetsnbextension' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 1398. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-widgetsnbextension-reference.md`
**Context:** The reference file for 'widgetsnbextension' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 1399. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-wininetpton-reference.md`
**Context:** The reference file for 'win_inet_pton' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 1400. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-wininetpton-reference.md`
**Context:** The reference file for 'win_inet_pton' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 1401. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-wininetpton-reference.md`
**Context:** The reference file for 'win_inet_pton' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 1402. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-winunicodeconsole-reference.md`
**Context:** The reference file for 'win_unicode_console' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 1403. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-winunicodeconsole-reference.md`
**Context:** The reference file for 'win_unicode_console' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 1404. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-winunicodeconsole-reference.md`
**Context:** The reference file for 'win_unicode_console' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 1405. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-wincertstore-reference.md`
**Context:** The reference file for 'wincertstore' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 1406. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-wincertstore-reference.md`
**Context:** The reference file for 'wincertstore' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 1407. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-wincertstore-reference.md`
**Context:** The reference file for 'wincertstore' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 1408. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-winpty-reference.md`
**Context:** The reference file for 'winpty' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 1409. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-winpty-reference.md`
**Context:** The reference file for 'winpty' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 1410. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-winpty-reference.md`
**Context:** The reference file for 'winpty' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 1411. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-wrapt-reference.md`
**Context:** The reference file for 'wrapt' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 1412. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-wrapt-reference.md`
**Context:** The reference file for 'wrapt' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 1413. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-wrapt-reference.md`
**Context:** The reference file for 'wrapt' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 1414. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-xarray-reference.md`
**Context:** The reference file for 'xarray' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 1415. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-xarray-reference.md`
**Context:** The reference file for 'xarray' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 1416. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-xarray-reference.md`
**Context:** The reference file for 'xarray' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 1417. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-xerces-c-reference.md`
**Context:** The reference file for 'xerces-c' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 1418. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-xerces-c-reference.md`
**Context:** The reference file for 'xerces-c' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 1419. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-xerces-c-reference.md`
**Context:** The reference file for 'xerces-c' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 1420. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-xlrd-reference.md`
**Context:** The reference file for 'xlrd' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 1421. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-xlrd-reference.md`
**Context:** The reference file for 'xlrd' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 1422. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-xlrd-reference.md`
**Context:** The reference file for 'xlrd' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 1423. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-xlsxwriter-reference.md`
**Context:** The reference file for 'xlsxwriter' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 1424. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-xlsxwriter-reference.md`
**Context:** The reference file for 'xlsxwriter' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 1425. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-xlsxwriter-reference.md`
**Context:** The reference file for 'xlsxwriter' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 1426. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-xlwings-reference.md`
**Context:** The reference file for 'xlwings' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 1427. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-xlwings-reference.md`
**Context:** The reference file for 'xlwings' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 1428. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-xlwings-reference.md`
**Context:** The reference file for 'xlwings' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 1429. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-xyzservices-reference.md`
**Context:** The reference file for 'xyzservices' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 1430. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-xyzservices-reference.md`
**Context:** The reference file for 'xyzservices' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 1431. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-xyzservices-reference.md`
**Context:** The reference file for 'xyzservices' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 1432. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-xz-reference.md`
**Context:** The reference file for 'xz' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 1433. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-xz-reference.md`
**Context:** The reference file for 'xz' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 1434. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-xz-reference.md`
**Context:** The reference file for 'xz' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 1435. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-yaml-reference.md`
**Context:** The reference file for 'yaml' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 1436. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-yaml-reference.md`
**Context:** The reference file for 'yaml' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 1437. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-yaml-reference.md`
**Context:** The reference file for 'yaml' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 1438. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-yapf-reference.md`
**Context:** The reference file for 'yapf' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 1439. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-yapf-reference.md`
**Context:** The reference file for 'yapf' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 1440. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-yapf-reference.md`
**Context:** The reference file for 'yapf' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 1441. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-yfinance-reference.md`
**Context:** The reference file for 'yfinance' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 1442. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-yfinance-reference.md`
**Context:** The reference file for 'yfinance' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 1443. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-yfinance-reference.md`
**Context:** The reference file for 'yfinance' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 1444. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-zeromq-reference.md`
**Context:** The reference file for 'zeromq' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 1445. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-zeromq-reference.md`
**Context:** The reference file for 'zeromq' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 1446. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-zeromq-reference.md`
**Context:** The reference file for 'zeromq' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 1447. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-zfp-reference.md`
**Context:** The reference file for 'zfp' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 1448. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-zfp-reference.md`
**Context:** The reference file for 'zfp' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 1449. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-zfp-reference.md`
**Context:** The reference file for 'zfp' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 1450. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-zict-reference.md`
**Context:** The reference file for 'zict' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 1451. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-zict-reference.md`
**Context:** The reference file for 'zict' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 1452. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-zict-reference.md`
**Context:** The reference file for 'zict' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 1453. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-zipp-reference.md`
**Context:** The reference file for 'zipp' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 1454. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-zipp-reference.md`
**Context:** The reference file for 'zipp' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 1455. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-zipp-reference.md`
**Context:** The reference file for 'zipp' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 1456. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-zlib-reference.md`
**Context:** The reference file for 'zlib' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 1457. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-zlib-reference.md`
**Context:** The reference file for 'zlib' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 1458. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-zlib-reference.md`
**Context:** The reference file for 'zlib' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 1459. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-zope-reference.md`
**Context:** The reference file for 'zope' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 1460. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-zope-reference.md`
**Context:** The reference file for 'zope' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 1461. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-zope-reference.md`
**Context:** The reference file for 'zope' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 1462. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-zopeinterface-reference.md`
**Context:** The reference file for 'zope.interface' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 1463. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-zopeinterface-reference.md`
**Context:** The reference file for 'zope.interface' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 1464. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-zopeinterface-reference.md`
**Context:** The reference file for 'zope.interface' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 1465. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-zstandard-reference.md`
**Context:** The reference file for 'zstandard' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 1466. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-zstandard-reference.md`
**Context:** The reference file for 'zstandard' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 1467. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-zstandard-reference.md`
**Context:** The reference file for 'zstandard' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 1468. `{TOOL_DOCS_URL}`

**Found in:** `references/ref-zstd-reference.md`
**Context:** The reference file for 'zstd' is missing a official documentation URL. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_DOCS_URL}` across all generated
agent files and replace with the correct value.

---

## 1469. `{TOOL_API_SURFACE}`

**Found in:** `references/ref-zstd-reference.md`
**Context:** The reference file for 'zstd' is missing a project-relevant API surface summary. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_API_SURFACE}` across all generated
agent files and replace with the correct value.

---

## 1470. `{TOOL_COMMON_PATTERNS}`

**Found in:** `references/ref-zstd-reference.md`
**Context:** The reference file for 'zstd' is missing a tool-specific usage patterns and pitfalls. Add it to the tools[] entry in the project brief or extend the built-in tool metadata catalog.

**Action required:** Search for `{MANUAL:TOOL_COMMON_PATTERNS}` across all generated
agent files and replace with the correct value.

---

## 1471. `{COMPONENT_SPEC}`

**Found in:** `crisis-credit-allocation-expert.agent.md`
**Context:** Component 'Crisis and Credit Allocation' is missing its component specification. Add the field to the component entry in the project brief so the expert brief renders completely.

**Action required:** Search for `{MANUAL:COMPONENT_SPEC}` across all generated
agent files and replace with the correct value.

---

## 1472. `{COMPONENT_SECTIONS}`

**Found in:** `crisis-credit-allocation-expert.agent.md`
**Context:** Component 'Crisis and Credit Allocation' is missing its section outline. Add the field to the component entry in the project brief so the expert brief renders completely.

**Action required:** Search for `{MANUAL:COMPONENT_SECTIONS}` across all generated
agent files and replace with the correct value.

---

## 1473. `{COMPONENT_SOURCES}`

**Found in:** `crisis-credit-allocation-expert.agent.md`
**Context:** Component 'Crisis and Credit Allocation' is missing its source list. Add the field to the component entry in the project brief so the expert brief renders completely.

**Action required:** Search for `{MANUAL:COMPONENT_SOURCES}` across all generated
agent files and replace with the correct value.

---

## 1474. `{COMPONENT_QUALITY_CRITERIA}`

**Found in:** `crisis-credit-allocation-expert.agent.md`
**Context:** Component 'Crisis and Credit Allocation' is missing its quality criteria list. Add the field to the component entry in the project brief so the expert brief renders completely.

**Action required:** Search for `{MANUAL:COMPONENT_QUALITY_CRITERIA}` across all generated
agent files and replace with the correct value.

---

## 1475. `{COMPONENT_SPEC}`

**Found in:** `fed-response-dag-expert.agent.md`
**Context:** Component 'Federal Reserve Response Function DAG Analysis' is missing its component specification. Add the field to the component entry in the project brief so the expert brief renders completely.

**Action required:** Search for `{MANUAL:COMPONENT_SPEC}` across all generated
agent files and replace with the correct value.

---

## 1476. `{COMPONENT_SECTIONS}`

**Found in:** `fed-response-dag-expert.agent.md`
**Context:** Component 'Federal Reserve Response Function DAG Analysis' is missing its section outline. Add the field to the component entry in the project brief so the expert brief renders completely.

**Action required:** Search for `{MANUAL:COMPONENT_SECTIONS}` across all generated
agent files and replace with the correct value.

---

## 1477. `{COMPONENT_SOURCES}`

**Found in:** `fed-response-dag-expert.agent.md`
**Context:** Component 'Federal Reserve Response Function DAG Analysis' is missing its source list. Add the field to the component entry in the project brief so the expert brief renders completely.

**Action required:** Search for `{MANUAL:COMPONENT_SOURCES}` across all generated
agent files and replace with the correct value.

---

## 1478. `{COMPONENT_QUALITY_CRITERIA}`

**Found in:** `fed-response-dag-expert.agent.md`
**Context:** Component 'Federal Reserve Response Function DAG Analysis' is missing its quality criteria list. Add the field to the component entry in the project brief so the expert brief renders completely.

**Action required:** Search for `{MANUAL:COMPONENT_QUALITY_CRITERIA}` across all generated
agent files and replace with the correct value.

---

## 1479. `{COMPONENT_SPEC}`

**Found in:** `prairie-prosperity-expert.agent.md`
**Context:** Component 'More Prairie Prosperity - Fiscal and Economic Policy in North Dakota' is missing its component specification. Add the field to the component entry in the project brief so the expert brief renders completely.

**Action required:** Search for `{MANUAL:COMPONENT_SPEC}` across all generated
agent files and replace with the correct value.

---

## 1480. `{COMPONENT_SECTIONS}`

**Found in:** `prairie-prosperity-expert.agent.md`
**Context:** Component 'More Prairie Prosperity - Fiscal and Economic Policy in North Dakota' is missing its section outline. Add the field to the component entry in the project brief so the expert brief renders completely.

**Action required:** Search for `{MANUAL:COMPONENT_SECTIONS}` across all generated
agent files and replace with the correct value.

---

## 1481. `{COMPONENT_SOURCES}`

**Found in:** `prairie-prosperity-expert.agent.md`
**Context:** Component 'More Prairie Prosperity - Fiscal and Economic Policy in North Dakota' is missing its source list. Add the field to the component entry in the project brief so the expert brief renders completely.

**Action required:** Search for `{MANUAL:COMPONENT_SOURCES}` across all generated
agent files and replace with the correct value.

---

## 1482. `{COMPONENT_QUALITY_CRITERIA}`

**Found in:** `prairie-prosperity-expert.agent.md`
**Context:** Component 'More Prairie Prosperity - Fiscal and Economic Policy in North Dakota' is missing its quality criteria list. Add the field to the component entry in the project brief so the expert brief renders completely.

**Action required:** Search for `{MANUAL:COMPONENT_QUALITY_CRITERIA}` across all generated
agent files and replace with the correct value.

---

## 1483. `{COMPONENT_SPEC}`

**Found in:** `sugarscape-expert.agent.md`
**Context:** Component 'Sugarscape Agent-Based Model' is missing its component specification. Add the field to the component entry in the project brief so the expert brief renders completely.

**Action required:** Search for `{MANUAL:COMPONENT_SPEC}` across all generated
agent files and replace with the correct value.

---

## 1484. `{COMPONENT_SECTIONS}`

**Found in:** `sugarscape-expert.agent.md`
**Context:** Component 'Sugarscape Agent-Based Model' is missing its section outline. Add the field to the component entry in the project brief so the expert brief renders completely.

**Action required:** Search for `{MANUAL:COMPONENT_SECTIONS}` across all generated
agent files and replace with the correct value.

---

## 1485. `{COMPONENT_SOURCES}`

**Found in:** `sugarscape-expert.agent.md`
**Context:** Component 'Sugarscape Agent-Based Model' is missing its source list. Add the field to the component entry in the project brief so the expert brief renders completely.

**Action required:** Search for `{MANUAL:COMPONENT_SOURCES}` across all generated
agent files and replace with the correct value.

---

## 1486. `{COMPONENT_QUALITY_CRITERIA}`

**Found in:** `sugarscape-expert.agent.md`
**Context:** Component 'Sugarscape Agent-Based Model' is missing its quality criteria list. Add the field to the component entry in the project brief so the expert brief renders completely.

**Action required:** Search for `{MANUAL:COMPONENT_QUALITY_CRITERIA}` across all generated
agent files and replace with the correct value.

---

## 1487. `{COMPONENT_SPEC}`

**Found in:** `visualize-energy-data-expert.agent.md`
**Context:** Component 'Visualize Energy Data' is missing its component specification. Add the field to the component entry in the project brief so the expert brief renders completely.

**Action required:** Search for `{MANUAL:COMPONENT_SPEC}` across all generated
agent files and replace with the correct value.

---

## 1488. `{COMPONENT_SECTIONS}`

**Found in:** `visualize-energy-data-expert.agent.md`
**Context:** Component 'Visualize Energy Data' is missing its section outline. Add the field to the component entry in the project brief so the expert brief renders completely.

**Action required:** Search for `{MANUAL:COMPONENT_SECTIONS}` across all generated
agent files and replace with the correct value.

---

## 1489. `{COMPONENT_SOURCES}`

**Found in:** `visualize-energy-data-expert.agent.md`
**Context:** Component 'Visualize Energy Data' is missing its source list. Add the field to the component entry in the project brief so the expert brief renders completely.

**Action required:** Search for `{MANUAL:COMPONENT_SOURCES}` across all generated
agent files and replace with the correct value.

---

## 1490. `{COMPONENT_QUALITY_CRITERIA}`

**Found in:** `visualize-energy-data-expert.agent.md`
**Context:** Component 'Visualize Energy Data' is missing its quality criteria list. Add the field to the component entry in the project brief so the expert brief renders completely.

**Action required:** Search for `{MANUAL:COMPONENT_QUALITY_CRITERIA}` across all generated
agent files and replace with the correct value.

---


Once all items above are resolved, invoke `@conflict-auditor` to verify consistency.
<!-- AGENTTEAMS:END content -->
